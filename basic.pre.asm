$Z:
.org $0000
STM = 0
KIM = 0
OSI = 0
COMMODORE = 0
APPLE = 1
REALIO = 4
INTPRC = 1
ADDPRC = 1
LNGERR = 0
TIME = 0
EXTIO = 0
DISKO = 0
NULCMD = 1
GETCMD = 1
RORSW = 1
ROMSW = 1
CLMWID = 14
LONGI = 1
STKEND = 511
BUFPAG = 0
LINLEN = 72
BUFLEN = 72
ROMLOC = 8192
KIMROM = 1
KIMROM = 0
RAMLOC = 16384
RORSW = 1
NULCMD = 0
GETCMD = 1
CQINLN = 64871
CQPRMP = 51
CQINCH = 64780
CQCOUT = 65229
CQCSIN = 65277
BUFPAG = 2
BUF = 512
ROMLOC = 2048
RAMLOC = 10752
OUTCH = 65005
CZGETL = 64780
LINLEN = 40
BUFLEN = 240
RORSW = 1
STKEND = 507
 JSR SYNCHR
 LDY #0
 LDX #0
 LDY #0
NUMLEV = 23
STRSIZ = 3
NUMTMP = 3
CONTW = 15
START: JMP INIT
RDYJSR: JMP INIT
ADRAYI: .word AYINT
ADRGAY: .word GIVAYF
USRPOK: JMP FCERR
CHARAC: .res 1
INTEGR = 0
ENDCHR: .res 1
COUNT: .res 1
DIMFLG: .res 1
KIMY = 0
VALTYP: .res 1
INTFLG: .res 1
DORES: .res 1
GARBFL = 0
SUBFLG: .res 1
INPFLG: .res 1
TANSGN: .res 1
CNTWFL: .res 1
.org $0050
TRMPOS: .res 1
LINWID: .byte LINLEN
NCMWID: .byte NCMPOS
LINNUM: .byte 0
.byte 44
TEMPPT: .res 1
LASTPT: .res 2
TEMPST: .res STRSIZ*NUMTMP
INDEX1: .res 2
INDEX = 0
INDEX2: .res 2
RESHO: .res 1
RESMOH: .res 1
RESMO: .res 1
RESLO: .res 1
ADDEND = 0
.byte 0
TXTTAB: .res 2
VARTAB: .res 2
ARYTAB: .res 2
STREND: .res 2
FRETOP: .res 2
FRESPC: .res 2
MEMSIZ: .res 2
CURLIN: .res 2
OLDLIN: .res 2
POKER = 0
OLDTXT: .res 2
DATLIN: .res 2
DATPTR: .res 2
INPPTR: .res 2
VARNAM: .res 2
VARPNT: .res 2
FDECPT = 0
FORPNT: .res 2
LSTPNT = 0
ANDMSK = 0
EORMSK = 1
OPPTR: .res 2
VARTXT = 0
OPMASK: .res 1
DOMASK = 0
DEFPNT: .res 2
GRBPNT = 0
DSCPNT: .res 2
.res 1
FOUR6: .word STRSIZ
JMPER: JMP 60000
SIZE = 1
OLDOV = 2
TEMPF3 = 0
TEMPF1:
.byte 0
HIGHDS: .res 2
HIGHTR: .res 2
TEMPF2:
.byte 0
LOWDS: .res 2
LOWTR: .res 2
ARYPNT = 0
GRBTOP = 0
DECCNT = 0
TENEXP = 1
DPTFLG = 0
EXPSGN = 1
FAC:
FACEXP: .byte 0
FACHO: .byte 0
FACMOH: .byte 0
FACMO: .byte 0
FACLO: .byte 0
FACSGN: .byte 0
SGNFLG: .byte 0
DEGREE = 0
DSCTMP = 0
INDICE = 0
BITS: .byte 0
ARGEXP: .byte 0
ARGHO: .byte 0
ARGMOH: .byte 0
ARGMO: .byte 0
ARGLO: .byte 0
ARGSGN: .byte 0
ARISGN: .byte 0
FACOV: .byte 0
STRNG1 = 0
FBUFPT: .res 2
BUFPTR = 0
STRNG2 = 0
POLYPT = 0
CURTOL = 0
CHRGET: INC CHRGET+7
 BNE CHRGOT
 INC CHRGET+8
CHRGOT: LDA 60000
TXTPTR = 1
 CMP #"
 BEQ CHRGET
QNUM: CMP #58
 BCS CHRRTS
 SEC:
 SBC #48
 SEC:
 SBC #208
CHRRTS: .byte RTS
RNDX: .byte 128
.byte 79
.byte 199
.byte 82
.byte 89
.org $00FF
LOFBUF: .res 1
FBUFFR: .res 3*ADDPRC+13
.org $0800
STMDSP: .word END-1
.word FOR-1
.word NEXT-1
.word DATA-1
.word INPUT-1
.word DIM-1
.word READ-1
.word LET-1
.word GOTO-1
.word RUN-1
.word IF-1
.word RESTORE-1
.word GOSUB-1
.word RETURN-1
.word REM-1
.word STOP-1
.word ONGOTO-1
.word FNWAIT-1
.word DEF-1
.word POKE-1
.word PRINT-1
.word CONT-1
.word LIST-1
.word CLEAR-1
.word GET-1
.word SCRATH-1
FUNDSP: .word SGN
.word INT
.word ABS
USRLOC: .word USRPOK
.word FRE
.word POS
.word SQR
.word RND
.word LOG
.word EXP
COSFIX: .word COS
SINFIX: .word SIN
TANFIX: .word TAN
ATNFIX: .word ATN
.word PEEK
.word LEN
.word STR
.word VAL
.word ASC
.word CHR
.word LEFT
.word RIGHT
.word MID
OPTAB: .byte 121
.word FADDT-1
.byte 121
.word FSUBT-1
.byte 123
.word FMULTT-1
.byte 123
.word FDIVT-1
.byte 127
.word FPWRT-1
.byte 80
.word ANDOP-1
.byte 70
.word OROP-1
NEGTAB: .byte 125
.word NEGOP-1
NOTTAB: .byte 90
.word NOTOP-1
PTDORL: .byte 100
.word DOREL-1
Q = 127
RESLST: .byte $45, $4E, $C4
ENDTK = 127
.byte $46, $4F, $D2
FORTK = 127
.byte $4E, $45, $58, $D4
.byte $44, $41, $54, $C1
DATATK = 127
.byte $49, $4E, $50, $55, $D4
.byte $44, $49, $CD
.byte $52, $45, $41, $C4
.byte $4C, $45, $D4
.byte $47, $4F, $54, $CF
GOTOTK = 127
.byte $52, $55, $CE
.byte $49, $C6
.byte $52, $45, $53, $54, $4F, $52, $C5
.byte $47, $4F, $53, $55, $C2
GOSUTK = 127
.byte $52, $45, $54, $55, $52, $CE
.byte $52, $45, $CD
REMTK = 127
.byte $53, $54, $4F, $D0
.byte $4F, $CE
.byte $57, $41, $49, $D4
.byte $44, $45, $C6
.byte $50, $4F, $4B, $C5
.byte $50, $52, $49, $4E, $D4
PRINTK = 127
.byte $43, $4F, $4E, $D4
.byte $4C, $49, $53, $D4
.byte $43, $4C, $45, $41, $D2
.byte $47, $45, $D4
.byte $4E, $45, $D7
SCRATK = 127
.byte $30
.byte $30
.byte $30
.byte $A8
Q = 128
TABTK = 128
.byte $54, $CF
TOTK = 128
.byte $46, $CE
FNTK = 128
.byte $30
.byte $30
.byte $30
.byte $A8
Q = 129
SPCTK = 129
.byte $54, $48, $45, $CE
THENTK = 129
.byte $4E, $4F, $D4
NOTTK = 129
.byte $53, $54, $45, $D0
STEPTK = 129
.byte $AB
PLUSTK = 129
.byte $AD
MINUTK = 129
.byte $AA
.byte $AF
.byte $DE
.byte $41, $4E, $C4
.byte $4F, $D2
.byte 190
Q = 130
GREATK = 130
.byte $BD
EQULTK = 130
.byte 188
Q = 131
LESSTK = 131
.byte $53, $47, $CE
ONEFUN = 131
.byte $49, $4E, $D4
.byte $41, $42, $D3
.byte $55, $53, $D2
.byte $46, $52, $C5
.byte $50, $4F, $D3
.byte $53, $51, $D2
.byte $52, $4E, $C4
.byte $4C, $4F, $C7
.byte $45, $58, $D0
.byte $43, $4F, $D3
.byte $53, $49, $CE
.byte $54, $41, $CE
.byte $41, $54, $CE
.byte $50, $45, $45, $CB
.byte $4C, $45, $CE
.byte $53, $54, $52, $A4
.byte $56, $41, $CC
.byte $41, $53, $C3
.byte $43, $48, $52, $A4
LASNUM = 131
.byte $4C, $45, $46, $54, $A4
.byte $52, $49, $47, $48, $54, $A4
.byte $4D, $49, $44, $A4
.byte $47, $CF
GOTK = 131
.byte 0
Q = -2
ERRTAB: .byte $4E, $46
ERRNF = Q
.byte $53, $4E
ERRSN = Q
.byte $52, $47
ERRRG = Q
.byte $4F, $44
ERROD = Q
.byte $46, $43
ERRFC = Q
.byte $4F, $56
ERROV = Q
.byte $4F, $4D
ERROM = Q
.byte $55, $53
ERRUS = Q
.byte $42, $53
ERRBS = Q
.byte $44, $44
ERRDD = Q
.byte $2F, $30
ERRDV0 = Q
.byte $49, $44
ERRID = Q
.byte $54, $4D
ERRTM = Q
.byte $4C, $53
ERRLS = Q
.byte $53, $54
ERRST = Q
.byte $43, $4E
ERRCN = Q
.byte $55, $46
ERRUF = Q
ERR: .byte $20, $45, $52, $52, $4F, $52
.byte 0
INTXT: .byte $20, $49, $4E, $20
.byte 0
REDDY: .byte ACRLF
.byte $4F, $4B
 ACRLF:
.byte 0
BRKTXT: .byte ACRLF
.byte $42, $52, $45, $41, $4B
.byte 0
FORSIZ = 18
FNDFOR: .byte TSX
 INX
 INX
 INX
 INX
FFLOOP: LDA 257,X
 CMP #127
 BNE FFRTS
 LDA FORPNT+1
 BNE CMPFOR
 LDA 258,X
 STA FORPNT
 LDA 259,X
 STA FORPNT+1
CMPFOR: CMP 259,X
 BNE ADDFRS
 LDA FORPNT
 CMP 258,X
 BEQ FFRTS
ADDFRS: .byte TXA
 CLC:
 ADC #18
 TAX:
 BNE FFLOOP
FFRTS: .byte RTS
BLTU: JSR REASON
 STA STREND
STY STREND+1
BLTUC: .byte SEC
 LDA HIGHTR
 SBC LOWTR
 STA INDEX
 TAY:
 LDA HIGHTR+1
 SBC LOWTR+1
 TAX:
 INX:
 TYA:
 BEQ DECBLT
 LDA HIGHTR
 SEC:
 SBC INDEX
 STA HIGHTR
 BCS BLT1
 DEC HIGHTR+1
 SEC:
BLT1: LDA HIGHDS
 SBC INDEX
 STA HIGHDS
 BCS MOREN1
 DEC HIGHDS+1
 BCC MOREN1
BLTLP: LDA (HIGHTR),Y
 STA (HIGHDS),Y
MOREN1: .byte DEY
 BNE BLTLP
 LDA (HIGHTR),Y
 STA (HIGHDS),Y
DECBLT: DEC HIGHTR+1
 DEC HIGHDS+1
 DEX:
 BNE MOREN1
 RTS:
GETSTK: ASL A,
 ADC #62
 BCS OMERR
 STA INDEX
 TSX:
 CPX INDEX
 BCC OMERR
 RTS:
REASON: CPY FRETOP+1
 BCC REARTS
 BNE TRYMOR
 CMP FRETOP
 BCC REARTS
TRYMOR: .byte PHA
 LDX #9
 TYA:
REASAV: .byte PHA
 LDA HIGHDS-1,X
 DEX:
 BPL REASAV
 JSR GARBA2
 LDX #247
REASTO: .byte PLA
 STA HIGHDS+8+ADDPRC,X
 INX:
 BMI REASTO
 PLA:
 TAY:
 PLA:
 CPY FRETOP+1
 BCC REARTS
 BNE OMERR
 CMP FRETOP
 BCS OMERR
REARTS: .byte RTS
OMERR: LDX #0
ERROR:
 LSR CNTWFL
ERRCRD: JSR CRDO
 JSR OUTQST
 LDA ERRTAB,X,
 JSR OUTDO
 LDA ERRTAB+1,X,
 JSR OUTDO
TYPERR: JSR STKINI
 LDA #<ERR
LDY #>ERR
ERRFIN: JSR STROUT
 LDY CURLIN+1
 INY:
 BEQ READY
 JSR INPRT
READY:
 LSR CNTWFL
 LDA #<REDDY
LDY #>REDDY
 JSR RDYJSR
MAIN: JSR INLIN
 STX TXTPTR
STY TXTPTR+1
 JSR CHRGET
 TAX:
 BEQ MAIN
 LDX #255
 STX CURLIN+1
 BCC MAIN1
 JSR CRUNCH
 JMP GONE
MAIN1: JSR LINGET
 JSR CRUNCH
 STY COUNT
 JSR FNDLIN
 BCC NODEL
 LDY #1
 LDA (LOWTR),Y
 STA INDEX1+1
 LDA VARTAB
 STA INDEX1
 LDA LOWTR+1
 STA INDEX2+1
 LDA LOWTR
 DEY:
 SBC (LOWTR),Y
 CLC:
 ADC VARTAB
 STA VARTAB
 STA INDEX2
 LDA VARTAB+1
 ADC #255
 STA VARTAB+1
 SBC LOWTR+1
 TAX:
 SEC:
 LDA LOWTR
 SBC VARTAB
 TAY:
 BCS QDECT1
 INX:
 DEC INDEX2+1
QDECT1: .byte CLC
 ADC INDEX1
 BCC MLOOP
 DEC INDEX1+1
 CLC:
MLOOP: LDA (INDEX1),Y
 STA (INDEX2),Y
 INY:
 BNE MLOOP
 INC INDEX1+1
 INC INDEX2+1
 DEX:
 BNE MLOOP
NODEL: JSR RUNC
 JSR LNKPRG
 LDA BUF
 BEQ MAIN
 CLC:
 LDA VARTAB
 STA HIGHTR
 ADC COUNT
 STA HIGHDS
 LDY VARTAB+1
 STY HIGHTR+1
 BCC NODELC
 INY:
NODELC: STY HIGHDS+1
 JSR BLTU
 LDA LINNUM
LDY LINNUM+1
 STA BUF-2
STY BUF-2+1
 LDA STREND
LDY STREND+1
 STA VARTAB
STY VARTAB+1
 LDY COUNT
 DEY:
STOLOP: LDA BUF-4,Y
 STA (LOWTR),Y
 DEY:
 BPL STOLOP
FINI: JSR RUNC
 JSR LNKPRG
 JMP MAIN
LNKPRG: LDA TXTTAB
LDY TXTTAB+1
 STA INDEX
STY INDEX+1
 CLC:
CHEAD: LDY #1
 LDA (INDEX),Y
 BEQ LNKRTS
 LDY #4
CZLOOP: .byte INY
 LDA (INDEX),Y
 BNE CZLOOP
 INY:
 TYA:
 ADC INDEX
 TAX:
 LDY #0
 STA (INDEX),Y
 LDA INDEX+1
 ADC #0
 INY:
 STA (INDEX),Y
 STX INDEX
 STA INDEX+1
 BCC CHEAD
LNKRTS: .byte RTS
INLIN: LDX #128
 STX CQPRMP
 JSR CQINLN
 CPX #239
 BCS GDBUFS
 LDX #239
GDBUFS: LDA #0
 STA BUF,X
 TXA:
 BEQ NOCHR
LOPBHT: LDA BUF-1,X
 AND #127
 STA BUF-1,X
 DEX:
 BNE LOPBHT
NOCHR: LDA #0
 LDX #<BUF-1
LDY #>BUF-1
 RTS:
INCHR:
 JSR CQINCH
 AND #127
 CMP #15
 BNE INCRTS
 PHA:
 LDA CNTWFL
EOR #$FF
STA CNTWFL
 PLA:
INCRTS: .byte RTS
BUFOFS = 0
BUFOFS = 512
CRUNCH: LDX TXTPTR
 LDY #4
 STY DORES
KLOOP: LDA BUFOFS,X
CMPSPC: CMP #"
 BEQ STUFFH
 STA ENDCHR
 CMP #34
 BEQ STRNG
 BIT DORES
 BVS STUFFH
 CMP #63
 BNE KLOOP1
 LDA #127
 BNE STUFFH
KLOOP1: CMP #48
 BCC MUSTCR
 CMP #60
 BCC STUFFH
MUSTCR: STY BUFPTR
 LDY #0
 STY COUNT
 DEY:
 STX TXTPTR
 DEX:
RESER: .byte INY
RESPUL: .byte INX
RESCON: LDA BUFOFS,X
 SEC:
 SBC RESLST,Y
 BEQ RESER
 CMP #128
 BNE NTHIS
 ORA COUNT
GETBPT: LDY BUFPTR
STUFFH: .byte INX
 INY:
 STA BUF-5,Y
 LDA BUF-5,Y
 BEQ CRDONE
 SEC:
 SBC #58
 BEQ COLIS
 CMP #69
 BNE NODATT
COLIS: STA DORES
NODATT: .byte SEC
 SBC #69
 BNE KLOOP
 STA ENDCHR
STR1: LDA BUFOFS,X
 BEQ STUFFH
 CMP ENDCHR
 BEQ STUFFH
STRNG: .byte INY
 STA BUF-5,Y
 INX:
 BNE STR1
NTHIS: LDX TXTPTR
 INC COUNT
NTHIS1: .byte INY
 LDA RESLST-1,Y,
 BPL NTHIS1
 LDA RESLST,Y,
 BNE RESCON
 LDA BUFOFS,X
 BPL GETBPT
CRDONE: STA BUF-3,Y,
 DEC TXTPTR+1
 LDA #255-1
 STA TXTPTR
LISTRT: .byte RTS
FNDLIN: LDA TXTTAB
LDX TXTTAB+1
FNDLNC: LDY #1
 STA LOWTR
STX LOWTR+1
 LDA (LOWTR),Y
 BEQ FLINRT
 INY:
 INY:
 LDA LINNUM+1
 CMP (LOWTR),Y
 BCC FLNRTS
 BEQ FNDLO1
 DEY:
 BNE AFFRTS
FNDLO1: LDA LINNUM
 DEY:
 CMP (LOWTR),Y
 BCC FLNRTS
 BEQ FLNRTS
AFFRTS: .byte DEY
 LDA (LOWTR),Y
 TAX:
 DEY:
 LDA (LOWTR),Y
 BCS FNDLNC
FLINRT: .byte CLC
FLNRTS: .byte RTS
SCRATH: BNE FLNRTS
SCRTCH: LDA #0
 TAY:
 STA (TXTTAB),Y
 INY:
 STA (TXTTAB),Y
 LDA TXTTAB
 CLC:
 ADC #2
 STA VARTAB
 LDA TXTTAB+1
 ADC #0
 STA VARTAB+1
RUNC: JSR STXTPT
 LDA #0
CLEAR: BNE STKRTS
CLEARC: LDA MEMSIZ
LDY MEMSIZ+1
 STA FRETOP
STY FRETOP+1
 LDA VARTAB
LDY VARTAB+1
 STA ARYTAB
STY ARYTAB+1
 STA STREND
STY STREND+1
FLOAD: JSR RESTOR
STKINI: LDX #0
 STX TEMPPT
 PLA:
 TAY:
 PLA:
 LDX #250
 TXS:
 PHA:
 TYA:
 PHA:
 LDA #0
 STA OLDTXT+1
 STA SUBFLG
STKRTS: .byte RTS
STXTPT: .byte CLC
 LDA TXTTAB
 ADC #255
 STA TXTPTR
 LDA TXTTAB+1
 ADC #255
 STA TXTPTR+1
 RTS:
LIST: BCC GOLST
 BEQ GOLST
 CMP #129
 BNE STKRTS
GOLST: JSR LINGET
 JSR FNDLIN
 JSR CHRGOT
 BEQ LSTEND
 CMP #129
 BNE FLNRTS
 JSR CHRGET
 JSR LINGET
 BNE FLNRTS
LSTEND: .byte PLA
 PLA:
 LDA LINNUM
 ORA LINNUM+1
 BNE LIST4
 LDA #255
 STA LINNUM
 STA LINNUM+1
LIST4: LDY #1
 LDA (LOWTR),Y
 BEQ GRODY
 JSR ISCNTC
 JSR CRDO
 INY:
 LDA (LOWTR),Y
 TAX:
 INY:
 LDA (LOWTR),Y
 CMP LINNUM+1
 BNE TSTDUN
 CPX LINNUM
 BEQ TYPLIN
TSTDUN: BCS GRODY
TYPLIN: STY LSTPNT
 JSR LINPRT
 LDA #"
PRIT4: LDY LSTPNT
 AND #127
PLOOP: JSR OUTDO
PLOOP1: .byte INY
 BEQ GRODY
 LDA (LOWTR),Y
 BNE QPLOP
 TAY:
 LDA (LOWTR),Y
 TAX:
 INY:
 LDA (LOWTR),Y
 STX LOWTR
 STA LOWTR+1
 BNE LIST4
GRODY: JMP READY
QPLOP: BPL PLOOP
 SEC:
 SBC #127
 TAX:
 STY LSTPNT
 LDY #255
RESRCH: .byte DEX
 BEQ PRIT3
RESCR1: .byte INY
 LDA RESLST,Y,
 BPL RESCR1
 BMI RESRCH
PRIT3: .byte INY
 LDA RESLST,Y
 BMI PRIT4
 JSR OUTDO
 BNE PRIT3
FOR: LDA #128
 STA SUBFLG
 JSR LET
 JSR FNDFOR
 BNE NOTOL
 TXA:
 ADC #15
 TAX:
 TXS:
NOTOL: .byte PLA
 PLA:
 LDA #9
 JSR GETSTK
 JSR DATAN
 CLC:
 TYA:
 ADC TXTPTR
 PHA:
 LDA TXTPTR+1
 ADC #0
 PHA:
 LDA CURLIN
PHA
LDA CURLIN+1
PHA
 LDA #TOTK
JSR SYNCHR
 JSR CHKNUM
 JSR FRMNUM
 LDA FACSGN
 ORA #127
 AND FACHO
 STA FACHO
 LDA #<LDFONE
LDY #>LDFONE
 STA INDEX1
STY INDEX1+1
 JMP FORPSH
LDFONE: LDA #<FONE
LDY #>FONE
 JSR MOVFM
 JSR CHRGOT
 CMP #129
 BNE ONEON
 JSR CHRGET
 JSR FRMNUM
ONEON: JSR SIGN
 JSR PUSHF
 LDA FORPNT
PHA
LDA FORPNT+1
PHA
NXTCON: LDA #127
 PHA:
NEWSTT:
 JSR ISCNTC
 LDA TXTPTR
LDY TXTPTR+1
 CPY #2
 BEQ DIRCON
 STA OLDTXT
STY OLDTXT+1
DIRCON:
 LDY #0
 LDA (TXTPTR),Y
 BNE MORSTS
 LDY #2
 LDA (TXTPTR),Y
 CLC:
 BNE __LJ0
 JMP ENDCON
__LJ0:
 INY:
 LDA (TXTPTR),Y
 STA CURLIN
 INY:
 LDA (TXTPTR),Y
 STA CURLIN+1
 TYA:
 ADC TXTPTR
 STA TXTPTR
 BCC GONE
 INC TXTPTR+1
GONE: JSR CHRGET
 JSR GONE3
 JMP NEWSTT
GONE3: BEQ ISCRTS
GONE2: SBC #127
 BCC GLET
 CMP #1
 BCS SNERRX
 ASL A,
 TAY:
 LDA STMDSP+1,Y
 PHA:
 LDA STMDSP,Y
 PHA:
 JMP CHRGET
GLET: JMP LET
MORSTS: CMP #58
 BEQ GONE
SNERR1: JMP SNERR
SNERRX: CMP #4
 BNE SNERR1
 JSR CHRGET
 LDA #TOTK
JSR SYNCHR
 JMP GOTO
RESTOR: .byte SEC
 LDA TXTTAB
 SBC #1
 LDY TXTTAB+1
 BCS RESFIN
 DEY:
RESFIN: STA DATPTR
STY DATPTR+1
ISCRTS: .byte RTS
ISCNTC: LDA $C000
 CMP #131
 BEQ ISCCAP
 RTS:
ISCCAP: JSR INCHR
 CMP #131
STOP: BCS STOPC
END: .byte CLC
STOPC: BNE CONTRT
 LDA TXTPTR
LDY TXTPTR+1
 LDX CURLIN+1
 INX:
 BEQ DIRIS
 STA OLDTXT
STY OLDTXT+1
STPEND: LDA CURLIN
LDY CURLIN+1
 STA OLDLIN
STY OLDLIN+1
DIRIS: .byte PLA
 PLA:
ENDCON: LDA #<BRKTXT
LDY #>BRKTXT
 LDX #0
 STX CNTWFL
 BCC GORDY
 JMP ERRFIN
GORDY: JMP READY
CONT: BNE CONTRT
 LDX #0
 LDY OLDTXT+1
 BNE __LJ1
 JMP ERROR
__LJ1:
 LDA OLDTXT
 STA TXTPTR
STY TXTPTR+1
 LDA OLDLIN
LDY OLDLIN+1
 STA CURLIN
STY CURLIN+1
CONTRT: .byte RTS
SAVE: .byte SEC
 LDA VARTAB
 SBC TXTTAB
 STA POKER
 LDA VARTAB+1
 SBC TXTTAB+1
 STA POKER+1
 JSR VARTIO
 JSR CQCOUT
 JSR PROGIO
 JMP CQCOUT
LOAD: JSR VARTIO
 JSR CQCSIN
 CLC:
 LDA TXTTAB
 ADC POKER
 STA VARTAB
 LDA TXTTAB+1
 ADC POKER+1
 STA VARTAB+1
 JSR PROGIO
 JSR CQCSIN
 LDA #<TPDONE
LDY #>TPDONE
 JSR STROUT
 JMP FINI
TPDONE: .byte $4C, $4F, $41, $44, $45, $44
.byte 0
VARTIO: LDA #<POKER
LDY #>POKER
 STA $3C
STY $3C+1
 LDA #2
 STA $3E
STY $3E+1
 RTS:
PROGIO: LDA TXTTAB
LDY TXTTAB+1
 STA $3C
STY $3C+1
 LDA VARTAB
LDY VARTAB+1
 STA $3E
STY $3E+1
 RTS:
RUN: BNE __LJ2
 JMP RUNC
__LJ2:
 JSR CLEARC
 JMP RUNC2
GOSUB: LDA #3
 JSR GETSTK
 LDA TXTPTR
PHA
LDA TXTPTR+1
PHA
 LDA CURLIN
PHA
LDA CURLIN+1
PHA
 LDA #127
 PHA:
RUNC2: JSR CHRGOT
 JSR GOTO
 JMP NEWSTT
GOTO: JSR LINGET
 JSR REMN
 LDA CURLIN+1
 CMP LINNUM+1
 BCS LUK4IT
 TYA:
 SEC:
 ADC TXTPTR
 LDX TXTPTR+1
 BCC LUKALL
 INX:
 BCS LUKALL
LUK4IT: LDA TXTTAB
LDX TXTTAB+1
LUKALL: JSR FNDLNC
QFOUND: BCC USERR
 LDA LOWTR
 SBC #1
 STA TXTPTR
 LDA LOWTR+1
 SBC #0
 STA TXTPTR+1
GORTS: .byte RTS
RETURN: BNE GORTS
 LDA #255
 STA FORPNT+1
 JSR FNDFOR
 TXS:
 CMP #127
 BEQ RETU1
 LDX #0
 SKIP2:
USERR: LDX #0
 JMP ERROR
SNERR2: JMP SNERR
RETU1: .byte PLA
 PLA
STA CURLIN+1
PLA
STA CURLIN
 PLA
STA TXTPTR+1
PLA
STA TXTPTR
DATA: JSR DATAN
ADDON: .byte TYA
 CLC:
 ADC TXTPTR
 STA TXTPTR
 BCC REMRTS
 INC TXTPTR+1
REMRTS: .byte RTS
DATAN: LDX #58
 SKIP2:
REMN: LDX #0
 STX CHARAC
 LDY #0
 STY ENDCHR
EXCHQT: LDA ENDCHR
 LDX CHARAC
 STA CHARAC
 STX ENDCHR
REMER: LDA (TXTPTR),Y
 BEQ REMRTS
 CMP ENDCHR
 BEQ REMRTS
 INY:
 CMP #34
 BNE REMER
 BEQ EXCHQT
IF: JSR FRMEVL
 JSR CHRGOT
 CMP #127
 BEQ OKGOTO
 LDA #THENTK
JSR SYNCHR
OKGOTO: LDA FACEXP
 BNE DOCOND
REM: JSR REMN
 BEQ ADDON
DOCOND: JSR CHRGOT
 BCS DOCO
 JMP GOTO
DOCO: JMP GONE3
ONGOTO: JSR GETBYT
 PHA:
 CMP #127
 BEQ ONGLOP
SNERR3: CMP #127
 BNE SNERR2
ONGLOP: DEC FACLO
 BNE ONGLP1
 PLA:
 JMP GONE2
ONGLP1: JSR CHRGET
 JSR LINGET
 CMP #44
 BEQ ONGLOP
 PLA:
ONGRTS: .byte RTS
LINGET: LDX #0
 STX LINNUM
 STX LINNUM+1
MORLIN: BCS ONGRTS
 SBC #47
 STA CHARAC
 LDA LINNUM+1
 STA INDEX
 CMP #25
 BCS SNERR3
 LDA LINNUM
 ASL A,
 ROL INDEX
 ASL A
 ROL INDEX
 ADC LINNUM
 STA LINNUM
 LDA INDEX
 ADC LINNUM+1
 STA LINNUM+1
 ASL LINNUM
 ROL LINNUM+1
 LDA LINNUM
 ADC CHARAC
 STA LINNUM
 BCC NXTLGC
 INC LINNUM+1
NXTLGC: JSR CHRGET
 JMP MORLIN
LET: JSR PTRGET
 STA FORPNT
STY FORPNT+1
 LDA #EQULTK
JSR SYNCHR
 LDA INTFLG
 PHA:
 LDA VALTYP
 PHA:
 JSR FRMEVL
 PLA:
 ROL A,
 JSR CHKVAL
 BNE COPSTR
COPNUM:
 PLA:
QINTGR: BPL COPFLT
 JSR ROUND
 JSR AYINT
 LDY #0
 LDA FACMO
 STA (FORPNT),Y
 INY:
 LDA FACLO
 STA (FORPNT),Y
 RTS:
COPFLT: JMP MOVVF
COPSTR:
PLA
INPCOM:
GETSPT: LDY #2
 LDA (FACMO),Y
 CMP FRETOP+1
 BCC DNTCPY
 BNE QVARIA
 DEY:
 LDA (FACMO),Y
 CMP FRETOP
 BCC DNTCPY
QVARIA: LDY FACLO
 CPY VARTAB+1
 BCC DNTCPY
 BNE COPY
 LDA FACMO
 CMP VARTAB
 BCS COPY
DNTCPY: LDA FACMO
LDY FACMO+1
 JMP COPYZC
COPY: LDY #0
 LDA (FACMO),Y
 JSR STRINI
 LDA DSCPNT
LDY DSCPNT+1
 STA STRNG1
STY STRNG1+1
 JSR MOVINS
 LDA #<DSCTMP
LDY #>DSCTMP
COPYZC: STA DSCPNT
STY DSCPNT+1
 JSR FRETMS
 LDY #0
 LDA (DSCPNT),Y
 STA (FORPNT),Y
 INY:
 LDA (DSCPNT),Y
 STA (FORPNT),Y
 INY:
 LDA (DSCPNT),Y
 STA (FORPNT),Y
 RTS:
STRDON: JSR STRPRT
NEWCHR: JSR CHRGOT
PRINT: BEQ CRDO
PRINTC: BEQ PRTRTS
 CMP #128
 BEQ TABER
 CMP #129
 CLC:
 BEQ TABER
 CMP #44
 BEQ COMPRT
 CMP #59
 BEQ NOTABR
 JSR FRMEVL
 BIT VALTYP
 BMI STRDON
 JSR FOUT
 JSR STRLIT
 LDY #0
 LDA (FACMO),Y
 CLC:
 ADC TRMPOS
 CMP LINWID
 BCC LINCHK
 JSR CRDO
LINCHK: JSR STRPRT
 JSR OUTSPC
 BNE NEWCHR
CRDO:
 LDA #13
 STA TRMPOS
 JSR OUTDO
 LDA #10
 JSR OUTDO
CRFIN:
 LDA #0
 STA TRMPOS
 EOR #255
PRTRTS: .byte RTS
COMPRT: LDA TRMPOS
NCMPOS = 14
 CMP NCMWID
 BCC MORCOM
 JSR CRDO
 JMP NOTABR
MORCOM: .byte SEC
MORCO1: SBC #14
 BCS MORCO1
 EOR #255
 ADC #1
 BNE ASPAC
TABER: .byte PHP
 JSR GTBYTC
 CMP #41
 BNE SNERR4
 PLP:
 BCC XSPAC
 TXA:
 SBC TRMPOS
 BCC NOTABR
ASPAC: .byte TAX
XSPAC: .byte INX
XSPAC2: .byte DEX
 BNE XSPAC1
NOTABR: JSR CHRGET
 JMP PRINTC
XSPAC1: JSR OUTSPC
 BNE XSPAC2
STROUT: JSR STRLIT
STRPRT: JSR FREFAC
 TAX:
 LDY #0
 INX:
STRPR2: .byte DEX
 BEQ PRTRTS
 LDA (INDEX),Y
 JSR OUTDO
 INY:
 CMP #13
 BNE STRPR2
 JSR CRFIN
 JMP STRPR2
OUTSPC:
 LDA #"
 SKIP2:
OUTQST: LDA #63
OUTDO:
 BIT CNTWFL
 BMI OUTRTS
 PHA:
 CMP #32
 BCC TRYOUT
 LDA TRMPOS
 CMP LINWID
 BNE OUTDO1
 JSR CRDO
OUTDO1:
INCTRM: INC TRMPOS
TRYOUT: .byte PLA
ORA #$80
OUTLOC: JSR OUTCH
AND #$7F
OUTRTS: AND #255
GETRTS: .byte RTS
TRMNOK: LDA INPFLG
 BEQ TRMNO1
 BMI GETDTL
 LDY #255
 BNE STCURL
GETDTL:
 LDA DATLIN
LDY DATLIN+1
STCURL: STA CURLIN
STY CURLIN+1
SNERR4: JMP SNERR
TRMNO1:
DOAGIN: LDA #<TRYAGN
LDY #>TRYAGN
 JSR STROUT
 LDA OLDTXT
LDY OLDTXT+1
 STA TXTPTR
STY TXTPTR+1
 RTS:
GET: JSR ERRDIR
GETTTY: LDX #<BUF+1
LDY #>BUF+1
 LDA #0
 STA BUF+1
 LDA #64
 JSR INPCO1
 RTS:
INPUT:
 LSR CNTWFL
 CMP #34
 BNE NOTQTI
 JSR STRTXT
 LDA #59
JSR SYNCHR
 JSR STRPRT
NOTQTI: JSR ERRDIR
 LDA #44
 STA BUF-1
GETAGN: JSR QINLIN
 LDA BUF
 BNE INPCON
 CLC:
 JMP STPEND
QINLIN:
 JSR OUTQST
 JSR OUTSPC
GINLIN: JMP INLIN
READ: LDX DATPTR
LDY DATPTR+1
 TYA:
 SKIP2:
INPCON: LDA #0
INPCO1: STA INPFLG
 STX INPPTR
STY INPPTR+1
INLOOP: JSR PTRGET
 STA FORPNT
STY FORPNT+1
 LDA TXTPTR
LDY TXTPTR+1
 STA VARTXT
STY VARTXT+1
 LDX INPPTR
LDY INPPTR+1
 STX TXTPTR
STY TXTPTR+1
 JSR CHRGOT
 BNE DATBK1
 BIT INPFLG
 BVC QDATA
 JSR CZGETL
 AND #127
 STA BUF
 LDX #<BUF-1
LDY #>BUF-1
 BNE DATBK
QDATA: BMI DATLOP
 JSR OUTQST
GETNTH: JSR QINLIN
DATBK: STX TXTPTR
STY TXTPTR+1
DATBK1: JSR CHRGET
 BIT VALTYP
 BPL NUMINS
 BIT INPFLG
 BVC SETQUT
 INX:
 STX TXTPTR
 LDA #0
 STA CHARAC
 BEQ RESETC
SETQUT: STA CHARAC
 CMP #34
 BEQ NOWGET
 LDA #58
 STA CHARAC
 LDA #44
RESETC: .byte CLC
NOWGET: STA ENDCHR
 LDA TXTPTR
LDY TXTPTR+1
 ADC #0
 BCC NOWGE1
 INY:
NOWGE1: JSR STRLT2
 JSR ST2TXT
 JSR INPCOM
 JMP STRDN2
NUMINS: JSR FIN
 LDA INTFLG
 JSR QINTGR
STRDN2: JSR CHRGOT
 BEQ TRMOK
 CMP #44
 BEQ __LJ3
 JMP TRMNOK
__LJ3:
TRMOK: LDA TXTPTR
LDY TXTPTR+1
 STA INPPTR
STY INPPTR+1
 LDA VARTXT
LDY VARTXT+1
 STA TXTPTR
STY TXTPTR+1
 JSR CHRGOT
 BEQ VAREND
 JSR CHKCOM
 JMP INLOOP
DATLOP: JSR DATAN
 INY:
 TAX:
 BNE NOWLIN
 LDX #0
 INY:
 LDA (TXTPTR),Y
 BEQ ERRGO5
 INY:
 LDA (TXTPTR),Y
 STA DATLIN
 INY:
 LDA (TXTPTR),Y
 INY:
 STA DATLIN+1
NOWLIN: LDA (TXTPTR),Y
 TAX:
 JSR ADDON
 CPX #127
 BNE DATLOP
 JMP DATBK1
VAREND: LDA INPPTR
LDY INPPTR+1
 LDX INPFLG
 BPL VARY0
 JMP RESFIN
VARY0: LDY #0
 LDA (INPPTR),Y
 BEQ INPRTS
 LDA #<EXIGNT
LDY #>EXIGNT
 JMP STROUT
INPRTS: .byte RTS
EXIGNT: .byte $3F, $45, $58, $54, $52, $41, $20, $49, $47, $4E, $4F, $52, $45, $44
 ACRLF:
.byte 0
TRYAGN: .byte $3F, $52, $45, $44, $4F, $20, $46, $52, $4F, $4D, $20, $53, $54, $41, $52, $54
 ACRLF:
.byte 0
NEXT: BNE GETFOR
 LDY #0
 BEQ STXFOR
GETFOR: JSR PTRGET
STXFOR: STA FORPNT
STY FORPNT+1
 JSR FNDFOR
 BEQ HAVFOR
 LDX #0
ERRGO5: BEQ ERRGO4
HAVFOR: .byte TXS
 TXA:
 CLC:
 ADC #4
 PHA:
 ADC #6
 STA INDEX2
 PLA:
 LDY #1
 JSR MOVFM
 TSX:
 LDA 257+7+ADDPRC,X,
 STA FACSGN
 LDA FORPNT
LDY FORPNT+1
 JSR FADD
 JSR MOVVF
 LDY #1
 JSR FCOMPN
 TSX:
 SEC:
 SBC 257+7+ADDPRC,X,
 BEQ LOOPDN
 LDA 2*ADDPRC+12+257,X
 STA CURLIN
 LDA 257+13+<2*ADDPRC>,X
 STA CURLIN+1
 LDA 2*ADDPRC+15+257,X
 STA TXTPTR
 LDA 2*ADDPRC+14+257,X
 STA TXTPTR+1
NEWSGO: JMP NEWSTT
LOOPDN: .byte TXA
 ADC #17
 TAX:
 TXS:
 JSR CHRGOT
 CMP #44
 BNE NEWSGO
 JSR CHRGET
 JSR GETFOR
FRMNUM: JSR FRMEVL
CHKNUM: .byte CLC
 SKIP1:
CHKSTR: .byte SEC
CHKVAL: BIT VALTYP
 BMI DOCSTR
 BCS CHKERR
CHKOK: .byte RTS
DOCSTR: BCS CHKOK
CHKERR: LDX #0
ERRGO4: JMP ERROR
FRMEVL: LDX TXTPTR
 BNE FRMEV1
 DEC TXTPTR+1
FRMEV1: DEC TXTPTR
 LDX #0
 SKIP1:
LPOPER: .byte PHA
 TXA:
 PHA:
 LDA #1
 JSR GETSTK
 JSR EVAL
 LDA #0
STA OPMASK
TSTOP: JSR CHRGOT
LOPREL: .byte SEC
 SBC #130
 BCC ENDREL
 CMP #2
 BCS ENDREL
 CMP #1
 ROL A,
 EOR #1
 EOR OPMASK
 CMP OPMASK
 BCC SNERR5
 STA OPMASK
 JSR CHRGET
 JMP LOPREL
ENDREL: LDX OPMASK
 BNE FINREL
 BCS QOP
 ADC #1
 BCC QOP
 ADC VALTYP
 BNE __LJ4
 JMP CAT
__LJ4:
 ADC #255
 STA INDEX1
 ASL A,
 ADC INDEX1
 TAY:
QPREC: .byte PLA
 CMP OPTAB,Y
 BCS QCHNUM
 JSR CHKNUM
DOPREC: .byte PHA
NEGPRC: JSR DOPRE1
 PLA:
 LDY OPPTR
 BPL QPREC1
 TAX:
 BEQ QOPGO
 BNE PULSTK
FINREL: LSR VALTYP
 TXA:
 ROL A,
 LDX TXTPTR
 BNE FINRE2
 DEC TXTPTR+1
FINRE2: DEC TXTPTR
 LDY #0
 STA OPMASK
 BNE QPREC
QPREC1: CMP OPTAB,Y
 BCS PULSTK
 BCC DOPREC
DOPRE1: LDA OPTAB+2,Y
 PHA:
 LDA OPTAB+1,Y
 PHA:
 JSR PUSHF1
 LDA OPMASK
 JMP LPOPER
SNERR5: JMP SNERR
PUSHF1: LDA FACSGN
 LDX OPTAB,Y,
PUSHF: .byte TAY
 PLA:
 STA INDEX1
 INC INDEX1
 PLA:
 STA INDEX1+1
 TYA:
 PHA:
FORPSH: JSR ROUND
 LDA FACLO
 PHA:
 LDA FACMO
 PHA:
 LDA FACMOH
 PHA:
 LDA FACHO
 PHA:
 LDA FACEXP
 PHA:
QOP: LDY #255
 PLA:
QOPGO: BEQ QOPRTS
QCHNUM: CMP #100
 BEQ UNPSTK
 JSR CHKNUM
UNPSTK: STY OPPTR
PULSTK: .byte PLA
 LSR A,
 STA DOMASK
 PLA:
 STA ARGEXP
 PLA:
 STA ARGHO
 PLA:
 STA ARGMOH
 PLA:
 STA ARGMO
 PLA:
 STA ARGLO
 PLA:
 STA ARGSGN
 EOR FACSGN
 STA ARISGN
QOPRTS: LDA FACEXP
UNPRTS: .byte RTS
EVAL: LDA #0
STA VALTYP
EVAL0: JSR CHRGET
 BCS EVAL2
EVAL1: JMP FIN
EVAL2: JSR ISLETC
 BCS ISVAR
QDOT: CMP #48
 BEQ EVAL1
 CMP #129
 BEQ DOMIN
 CMP #129
 BEQ EVAL0
 CMP #34
 BNE EVAL3
STRTXT: LDA TXTPTR
LDY TXTPTR+1
 ADC #0
 BCC STRTX2
 INY:
STRTX2: JSR STRLIT
 JMP ST2TXT
EVAL3: CMP #129
 BNE EVAL4
 LDY #0
 BNE GONPRC
NOTOP: JSR AYINT
 LDA FACLO
 EOR #255
 TAY:
 LDA FACMO
 EOR #255
 JMP GIVAYF
EVAL4: CMP #128
 BNE __LJ5
 JMP FNDOER
__LJ5:
 CMP #131
 BCC PARCHK
 JMP ISFUN
PARCHK: JSR CHKOPN
 JSR FRMEVL
CHKCLS: LDA #41
 SKIP2:
CHKOPN: LDA #40
 SKIP2:
CHKCOM: LDA #44
SYNCHR: LDY #0
 CMP (TXTPTR),Y
 BNE SNERR
CHRGO5: JMP CHRGET
SNERR: LDX #0
 JMP ERROR
DOMIN: LDY #0
GONPRC: .byte PLA
 PLA:
 JMP NEGPRC
ISVAR: JSR PTRGET
ISVRET: STA FACMO
STY FACMO+1
 LDX VALTYP
 BEQ GOOO
 LDX #0
 STX FACOV
STRRTS: .byte RTS
GOOO:
 LDX INTFLG
 BPL GOOOOO
 LDY #0
 LDA (FACMO),Y
 TAX:
 INY:
 LDA (FACMO),Y
 TAY:
 TXA:
 JMP GIVAYF
GOOOOO:
QSTATV:
 JMP MOVFM
ISFUN: ASL A,
 PHA:
 TAX:
 JSR CHRGET
 CPX #7
 BCC OKNORM
 JSR CHKOPN
 JSR FRMEVL
 JSR CHKCOM
 JSR CHKSTR
 PLA:
 TAX:
 LDA FACMO
PHA
LDA FACMO+1
PHA
 TXA:
 PHA:
 JSR GETBYT
 PLA:
 TAY:
 TXA:
 PHA:
 JMP FINGO
OKNORM: JSR PARCHK
 PLA:
 TAY:
FINGO: LDA FUNDSP-2*ONEFUN+256,Y,
 STA JMPER+1
 LDA FUNDSP-2*ONEFUN+257,Y
 STA JMPER+2
 JSR JMPER
 JMP CHKNUM
OROP: LDY #255
 SKIP2:
ANDOP: LDY #0
 STY COUNT
 JSR AYINT
 LDA FACMO
 EOR COUNT
 STA INTEGR
 LDA FACLO
 EOR COUNT
 STA INTEGR+1
 JSR MOVFA
 JSR AYINT
 LDA FACLO
 EOR COUNT
 AND INTEGR+1
 EOR COUNT
 TAY:
 LDA FACMO
 EOR COUNT
 AND INTEGR
 EOR COUNT
 JMP GIVAYF
DOREL: JSR CHKVAL
 BCS STRCMP
 LDA ARGSGN
 ORA #127
 AND ARGHO
 STA ARGHO
 LDA #<ARGEXP
LDY #>ARGEXP
 JSR FCOMP
 TAX:
 JMP QCOMP
STRCMP: LDA #0
STA VALTYP
 DEC OPMASK
 JSR FREFAC
 STA DSCTMP
 STX DSCTMP+1
STY DSCTMP+1+1
 LDA ARGMO
LDY ARGMO+1
 JSR FRETMP
 STX ARGMO
STY ARGMO+1
 TAX:
 SEC:
 SBC DSCTMP
 BEQ STASGN
 LDA #1
 BCC STASGN
 LDX DSCTMP
 LDA #255
STASGN: STA FACSGN
 LDY #255
 INX:
NXTCMP: .byte INY
 DEX:
 BNE GETCMP
 LDX FACSGN
QCOMP: BMI DOCMP
 CLC:
 BCC DOCMP
GETCMP: LDA (ARGMO),Y
 CMP (DSCTMP+1),Y
 BEQ NXTCMP
 LDX #255
 BCS DOCMP
 LDX #1
DOCMP: .byte INX
 TXA:
 ROL A
 AND DOMASK
 BEQ GOFLOT
 LDA #255
GOFLOT: JMP FLOAT
DIM3: JSR CHKCOM
DIM: .byte TAX
DIM1: JSR PTRGT1
DIMCON: JSR CHRGOT
 BNE DIM3
 RTS:
PTRGET: LDX #0
 JSR CHRGOT
PTRGT1: STX DIMFLG
PTRGT2: STA VARNAM
 JSR CHRGOT
 JSR ISLETC
 BCS PTRGT3
INTERR: JMP SNERR
PTRGT3: LDX #0
 STX VALTYP
 STX INTFLG
 JSR CHRGET
 BCC ISSEC
 JSR ISLETC
 BCC NOSEC
ISSEC: .byte TAX
EATEM: JSR CHRGET
 BCC EATEM
 JSR ISLETC
 BCS EATEM
NOSEC: CMP #48
 BNE NOTSTR
 LDA #255
 STA VALTYP
 BNE TURNON
NOTSTR: CMP #37
 BNE STRNAM
 LDA SUBFLG
 BNE INTERR
 LDA #128
 STA INTFLG
 ORA VARNAM
 STA VARNAM
TURNON: .byte TXA
 ORA #128
 TAX:
 JSR CHRGET
STRNAM: STX VARNAM+1
 SEC:
 ORA SUBFLG
 SBC #40
 BNE __LJ6
 JMP ISARY
__LJ6:
 LDA #0
STA SUBFLG
 LDA VARTAB
 LDX VARTAB+1
 LDY #0
STXFND: STX LOWTR+1
LOPFND: STA LOWTR
 CPX ARYTAB+1
 BNE LOPFN
 CMP ARYTAB
 BEQ NOTFNS
LOPFN: LDA VARNAM
 CMP (LOWTR),Y
 BNE NOTIT
 LDA VARNAM+1
 INY:
 CMP (LOWTR),Y
 BEQ FINPTR
 DEY:
NOTIT: .byte CLC
 LDA LOWTR
 ADC #7
 BCC LOPFND
 INX:
 BNE STXFND
ISLETC: CMP #48
 BCC ISLRTS
 SBC #49
 SEC:
 SBC #207
ISLRTS: .byte RTS
NOTFNS: .byte PLA
 PHA:
 CMP #ISVRET-1-<ISVRET-1>/256*256
 BNE NOTEVL
 TSX:
 LDA 258,X
 CMP #>ISVRET-1
 BNE NOTEVL
LDZR: LDA #<ZERO
LDY #>ZERO
 RTS:
NOTEVL:
QSTAVR:
VAROK: LDA ARYTAB
LDY ARYTAB+1
 STA LOWTR
STY LOWTR+1
 LDA STREND
LDY STREND+1
 STA HIGHTR
STY HIGHTR+1
 CLC:
 ADC #7
 BCC NOTEVE
 INY:
NOTEVE: STA HIGHDS
STY HIGHDS+1
 JSR BLTU
 LDA HIGHDS
LDY HIGHDS+1
 INY:
 STA ARYTAB
STY ARYTAB+1
 LDY #0
 LDA VARNAM
 STA (LOWTR),Y
 INY:
 LDA VARNAM+1
 STA (LOWTR),Y
 LDA #0
 INY:
 STA (LOWTR),Y
 INY:
 STA (LOWTR),Y
 INY:
 STA (LOWTR),Y
 INY:
 STA (LOWTR),Y
 INY:
 STA (LOWTR),Y
FINPTR: LDA LOWTR
 CLC:
 ADC #2
 LDY LOWTR+1
 BCC FINNOW
 INY:
FINNOW: STA VARPNT
STY VARPNT+1
 RTS:
FMAPTR: LDA COUNT
 ASL A,
 ADC #5
 ADC LOWTR
 LDY LOWTR+1
 BCC JSRGM
 INY:
JSRGM: STA ARYPNT
STY ARYPNT+1
 RTS:
N32768: .byte $90, $80, $00, $00
INTIDX: JSR CHRGET
 JSR FRMEVL
POSINT: JSR CHKNUM
 LDA FACSGN
 BMI NONONO
AYINT: LDA FACEXP
 CMP #144
 BCC QINTGO
 LDA #<N32768
LDY #>N32768
 JSR FCOMP
NONONO: BNE FCERR
QINTGO: JMP QINT
ISARY: LDA DIMFLG
 ORA INTFLG
 PHA:
 LDA VALTYP
 PHA:
 LDY #0
INDLOP: .byte TYA
 PHA:
 LDA VARNAM
PHA
LDA VARNAM+1
PHA
 JSR INTIDX
 PLA
STA VARNAM+1
PLA
STA VARNAM
 PLA:
 TAY:
 TSX:
 LDA 258,X
 PHA:
 LDA 257,X
 PHA:
 LDA INDICE
 STA 258,X,
 LDA INDICE+1
 STA 257,X
 INY:
 JSR CHRGOT
 CMP #44
 BEQ INDLOP
 STY COUNT
 JSR CHKCLS
 PLA:
 STA VALTYP
 PLA:
 STA INTFLG
 AND #127
 STA DIMFLG
 LDX ARYTAB
 LDA ARYTAB+1
LOPFDA: STX LOWTR
 STA LOWTR+1
 CMP STREND+1
 BNE LOPFDV
 CPX STREND
 BEQ NOTFDD
LOPFDV: LDY #0
 LDA (LOWTR),Y
 INY:
 CMP VARNAM
 BNE NMARY1
 LDA VARNAM+1
 CMP (LOWTR),Y
 BEQ GOTARY
NMARY1: .byte INY
 LDA (LOWTR),Y
 CLC:
 ADC LOWTR
 TAX:
 INY:
 LDA (LOWTR),Y
 ADC LOWTR+1
 BCC LOPFDA
BSERR: LDX #0
 SKIP2:
FCERR: LDX #0
ERRGO3: JMP ERROR
GOTARY: LDX #0
 LDA DIMFLG
 BNE ERRGO3
 JSR FMAPTR
 LDA COUNT
 LDY #4
 CMP (LOWTR),Y
 BNE BSERR
 JMP GETDEF
NOTFDD: JSR FMAPTR
 JSR REASON
 LDA #0
 TAY:
 STA CURTOL+1
 LDX #5
 LDA VARNAM
 STA (LOWTR),Y
 BPL NOTFLT
 DEX:
NOTFLT: .byte INY
 LDA VARNAM+1
 STA (LOWTR),Y
 BPL STOMLT
 DEX:
 DEX:
STOMLT: STX CURTOL
 LDA COUNT
 INY
 INY
 INY
 STA (LOWTR),Y
LOPPTA: LDX #11
 LDA #0
 BIT DIMFLG
 BVC NOTDIM
 PLA:
 CLC:
 ADC #1
 TAX:
 PLA:
 ADC #0
NOTDIM: .byte INY
 STA (LOWTR),Y
 INY:
 TXA:
 STA (LOWTR),Y
 JSR UMULT
 STX CURTOL
 STA CURTOL+1
 LDY INDEX
 DEC COUNT
 BNE LOPPTA
 ADC ARYPNT+1
 BCS OMERR1
 STA ARYPNT+1
 TAY:
 TXA:
 ADC ARYPNT
 BCC GREASE
 INY:
 BEQ OMERR1
GREASE: JSR REASON
 STA STREND
STY STREND+1
 LDA #0
 INC CURTOL+1
 LDY CURTOL
 BEQ DECCUR
ZERITA: .byte DEY
 STA (ARYPNT),Y
 BNE ZERITA
DECCUR: DEC ARYPNT+1
 DEC CURTOL+1
 BNE ZERITA
 INC ARYPNT+1
 SEC:
 LDA STREND
 SBC LOWTR
 LDY #2
 STA (LOWTR),Y
 LDA STREND+1
 INY:
 SBC LOWTR+1
 STA (LOWTR),Y
 LDA DIMFLG
 BNE DIMRTS
 INY:
GETDEF: LDA (LOWTR),Y
 STA COUNT
 LDA #0
 STA CURTOL
INLPNM: STA CURTOL+1
 INY:
 PLA:
 TAX:
 STA INDICE
 PLA:
 STA INDICE+1
 CMP (LOWTR),Y
 BCC INLPN2
 BNE BSERR7
 INY:
 TXA:
 CMP (LOWTR),Y
 BCC INLPN1
BSERR7: JMP BSERR
OMERR1: JMP OMERR
INLPN2: .byte INY
INLPN1: LDA CURTOL+1
 ORA CURTOL
 CLC:
 BEQ ADDIND
 JSR UMULT
 TXA:
 ADC INDICE
 TAX:
 TYA:
 LDY INDEX1
ADDIND: ADC INDICE+1
 STX CURTOL
 DEC COUNT
 BNE INLPNM
 STA CURTOL+1
 LDX #5
 LDA VARNAM
 BPL NOTFL1
 DEX:
NOTFL1: LDA VARNAM+1
 BPL STOML1
 DEX:
 DEX:
STOML1: STX ADDEND
 LDA #0
 JSR UMULTD
 TXA:
 ADC ARYPNT
 STA VARPNT
 TYA:
 ADC ARYPNT+1
 STA VARPNT+1
 TAY:
 LDA VARPNT
DIMRTS: .byte RTS
UMULT: STY INDEX
 LDA (LOWTR),Y
 STA ADDEND
 DEY:
 LDA (LOWTR),Y
UMULTD: STA ADDEND+1
 LDA #16
 STA DECCNT
 LDX #0
 LDY #0
UMULTC: .byte TXA
 ASL A,
 TAX:
 TYA:
 ROL A,
 TAY:
 BCS OMERR1
 ASL CURTOL
 ROL CURTOL+1
 BCC UMLCNT
 CLC:
 TXA:
 ADC ADDEND
 TAX:
 TYA:
 ADC ADDEND+1
 TAY:
 BCS OMERR1
UMLCNT: DEC DECCNT
 BNE UMULTC
UMLRTS: .byte RTS
FRE: LDA VALTYP
 BEQ NOFREF
 JSR FREFAC
NOFREF: JSR GARBA2
 SEC:
 LDA FRETOP
 SBC STREND
 TAY:
 LDA FRETOP+1
 SBC STREND+1
GIVAYF: LDX #0
 STX VALTYP
 STA FACHO
STY FACHO+1
 LDX #144
 JMP FLOATS
POS: LDY TRMPOS
SNGFLT: LDA #0
 BEQ GIVAYF
ERRDIR: LDX CURLIN+1
 INX:
 BNE DIMRTS
 LDX #0
 SKIP2:
ERRGUF: LDX #0
ERRGO1: JMP ERROR
DEF: JSR GETFNM
 JSR ERRDIR
 JSR CHKOPN
 LDA #128
 STA SUBFLG
 JSR PTRGET
 JSR CHKNUM
 JSR CHKCLS
 LDA #EQULTK
JSR SYNCHR
PHA
 LDA VARPNT
PHA
LDA VARPNT+1
PHA
 LDA TXTPTR
PHA
LDA TXTPTR+1
PHA
 JSR DATA
 JMP DEFFIN
GETFNM: LDA #FNTK
JSR SYNCHR
 ORA #128
 STA SUBFLG
 JSR PTRGT2
 STA DEFPNT
STY DEFPNT+1
 JMP CHKNUM
FNDOER: JSR GETFNM
 LDA DEFPNT
PHA
LDA DEFPNT+1
PHA
 JSR PARCHK
 JSR CHKNUM
 PLA
STA DEFPNT+1
PLA
STA DEFPNT
 LDY #2
 LDA (DEFPNT),Y
 STA VARPNT
 TAX:
 INY:
 LDA (DEFPNT),Y
 BEQ ERRGUF
 STA VARPNT+1
INY
DEFSTF: LDA (VARPNT),Y
 PHA:
 DEY:
 BPL DEFSTF
 LDY VARPNT+1
 JSR MOVMF
 LDA TXTPTR
PHA
LDA TXTPTR+1
PHA
 LDA (DEFPNT),Y
 STA TXTPTR
 INY:
 LDA (DEFPNT),Y
 STA TXTPTR+1
 LDA VARPNT
PHA
LDA VARPNT+1
PHA
 JSR FRMNUM
 PLA
STA DEFPNT+1
PLA
STA DEFPNT
 JSR CHRGOT
 BEQ __LJ7
 JMP SNERR
__LJ7:
 PLA
STA TXTPTR+1
PLA
STA TXTPTR
DEFFIN: LDY #0
 PLA:
 STA (DEFPNT),Y
 PLA:
 INY:
 STA (DEFPNT),Y
 PLA:
 INY:
 STA (DEFPNT),Y
 PLA:
 INY:
 STA (DEFPNT),Y
 PLA:
 INY:
 STA (DEFPNT),Y
DEFRTS: .byte RTS
STR: JSR CHKNUM
 LDY #0
 JSR FOUTC
 PLA:
 PLA:
TIMSTR: LDA #<LOFBUF
LDY #>LOFBUF
 BEQ STRLIT
STRINI: LDX FACMO
LDY FACMO+1
 STX DSCPNT
STY DSCPNT+1
STRSPA: JSR GETSPA
 STX DSCTMP+1
STY DSCTMP+1+1
 STA DSCTMP
 RTS:
STRLIT: LDX #34
 STX CHARAC
 STX ENDCHR
STRLT2: STA STRNG1
STY STRNG1+1
 STA DSCTMP+1
STY DSCTMP+1+1
 LDY #255
STRGET: .byte INY
 LDA (STRNG1),Y
 BEQ STRFI1
 CMP CHARAC
 BEQ STRFIN
 CMP ENDCHR
 BNE STRGET
STRFIN: CMP #34
 BEQ STRFI2
STRFI1: .byte CLC
STRFI2: STY DSCTMP
 TYA:
 ADC STRNG1
 STA STRNG2
 LDX STRNG1+1
 BCC STRST2
 INX:
STRST2: STX STRNG2+1
 LDA STRNG1+1
 BEQ STRCP
 CMP #2
 BNE PUTNEW
STRCP: .byte TYA
 JSR STRINI
 LDX STRNG1
LDY STRNG1+1
 JSR MOVSTR
PUTNEW: LDX TEMPPT
 CPX #9
 BNE PUTNW1
 LDX #0
ERRGO2: JMP ERROR
PUTNW1: LDA DSCTMP
 STA 0,X
 LDA DSCTMP+1
 STA 1,X
 LDA DSCTMP+2
 STA 2,X
 LDY #0
 STX FACMO
STY FACMO+1
 STY FACOV
 DEY:
 STY VALTYP
 STX LASTPT
 INX:
 INX:
 INX:
 STX TEMPPT
 RTS:
GETSPA: LSR GARBFL
TRYAG2: .byte PHA
 EOR #255
 SEC:
 ADC FRETOP
 LDY FRETOP+1
 BCS TRYAG3
 DEY:
TRYAG3: CPY STREND+1
 BCC GARBAG
 BNE STRFRE
 CMP STREND
 BCC GARBAG
STRFRE: STA FRETOP
STY FRETOP+1
 STA FRESPC
STY FRESPC+1
 TAX:
 PLA:
 RTS:
GARBAG: LDX #0
 LDA GARBFL
 BMI ERRGO2
 JSR GARBA2
 LDA #128
 STA GARBFL
 PLA:
 BNE TRYAG2
GARBA2:
 LDA #7
 JSR OUTDO
 LDX MEMSIZ
 LDA MEMSIZ+1
FNDVAR: STX FRETOP
 STA FRETOP+1
 LDY #0
 STY GRBPNT+1
 STY GRBPNT
 LDA STREND
LDX STREND+1
 STA GRBTOP
STX GRBTOP+1
 LDA #<TEMPST
LDX #>TEMPST
 STA INDEX1
STX INDEX1+1
TVAR: CMP TEMPPT
 BEQ SVARS
 JSR DVAR
 BEQ TVAR
SVARS: LDA #7
 STA FOUR6
 LDA VARTAB
LDX VARTAB+1
 STA INDEX1
STX INDEX1+1
SVAR: CPX ARYTAB+1
 BNE SVARGO
 CMP ARYTAB
 BEQ ARYVAR
SVARGO: JSR DVARS
 BEQ SVAR
ARYVAR: STA ARYPNT
STX ARYPNT+1
 LDA #3
 STA FOUR6
ARYVA2: LDA ARYPNT
LDX ARYPNT+1
ARYVA3: CPX STREND+1
 BNE ARYVGO
 CMP STREND
 BNE __LJ8
 JMP GRBPAS
__LJ8:
ARYVGO: STA INDEX1
STX INDEX1+1
 LDY #0
 LDA (INDEX1),Y
 TAX:
 INY:
 LDA (INDEX1),Y
 PHP:
 INY:
 LDA (INDEX1),Y
 ADC ARYPNT
 STA ARYPNT
 INY:
 LDA (INDEX1),Y
 ADC ARYPNT+1
 STA ARYPNT+1
 PLP:
 BPL ARYVA2
 TXA:
 BMI ARYVA2
 INY:
 LDA (INDEX1),Y
 LDY #0
 ASL A,
 ADC #5
 ADC INDEX1
 STA INDEX1
 BCC ARYGET
 INC INDEX1+1
ARYGET: LDX INDEX1+1
ARYSTR: CPX ARYPNT+1
 BNE GOGO
 CMP ARYPNT
 BEQ ARYVA3
GOGO: JSR DVAR
 BEQ ARYSTR
DVARS:
 LDA (INDEX1),Y
 BMI DVARTS
 INY:
 LDA (INDEX1),Y
 BPL DVARTS
 INY:
DVAR: LDA (INDEX1),Y
 BEQ DVARTS
 INY:
 LDA (INDEX1),Y
 TAX:
 INY:
 LDA (INDEX1),Y
 CMP FRETOP+1
 BCC DVAR2
 BNE DVARTS
 CPX FRETOP
 BCS DVARTS
DVAR2: CMP GRBTOP+1
 BCC DVARTS
 BNE DVAR3
 CPX GRBTOP
 BCC DVARTS
DVAR3: STX GRBTOP
 STA GRBTOP+1
 LDA INDEX1
LDX INDEX1+1
 STA GRBPNT
STX GRBPNT+1
 LDA FOUR6
 STA SIZE
DVARTS: LDA FOUR6
 CLC:
 ADC INDEX1
 STA INDEX1
 BCC GRBRTS
 INC INDEX1+1
GRBRTS: LDX INDEX1+1
 LDY #0
 RTS:
GRBPAS: LDA GRBPNT+1
 ORA GRBPNT
 BEQ GRBRTS
 LDA SIZE
 AND #4
 LSR A,
 TAY:
 STA SIZE
 LDA (GRBPNT),Y
 ADC LOWTR
 STA HIGHTR
 LDA LOWTR+1
 ADC #0
 STA HIGHTR+1
 LDA FRETOP
LDX FRETOP+1
 STA HIGHDS
STX HIGHDS+1
 JSR BLTUC
 LDY SIZE
 INY:
 LDA HIGHDS
 STA (GRBPNT),Y
 TAX:
 INC HIGHDS+1
 LDA HIGHDS+1
 INY:
 STA (GRBPNT),Y
 JMP FNDVAR
CAT: LDA FACLO
 PHA:
 LDA FACMO
 PHA:
 JSR EVAL
 JSR CHKSTR
 PLA:
 STA STRNG1
 PLA:
 STA STRNG1+1
 LDY #0
 LDA (STRNG1),Y
 CLC:
 ADC (FACMO),Y
 BCC SIZEOK
 LDX #0
 JMP ERROR
SIZEOK: JSR STRINI
 JSR MOVINS
 LDA DSCPNT
LDY DSCPNT+1
 JSR FRETMP
 JSR MOVDO
 LDA STRNG1
LDY STRNG1+1
 JSR FRETMP
 JSR PUTNEW
 JMP TSTOP
MOVINS: LDY #0
 LDA (STRNG1),Y
 PHA:
 INY:
 LDA (STRNG1),Y
 TAX:
 INY:
 LDA (STRNG1),Y
 TAY:
 PLA:
MOVSTR: STX INDEX
STY INDEX+1
MOVDO: .byte TAY
 BEQ MVDONE
 PHA:
MOVLP: .byte DEY
 LDA (INDEX),Y
 STA (FRESPC),Y
QMOVE: .byte TYA
 BNE MOVLP
 PLA:
MVDONE: .byte CLC
 ADC FRESPC
 STA FRESPC
 BCC MVSTRT
 INC FRESPC+1
MVSTRT: .byte RTS
FRESTR: JSR CHKSTR
FREFAC: LDA FACMO
LDY FACMO+1
FRETMP: STA INDEX
STY INDEX+1
 JSR FRETMS
 PHP:
 LDY #0
 LDA (INDEX),Y
 PHA:
 INY:
 LDA (INDEX),Y
 TAX:
 INY:
 LDA (INDEX),Y
 TAY:
 PLA:
 PLP:
 BNE FRETRT
 CPY FRETOP+1
 BNE FRETRT
 CPX FRETOP
 BNE FRETRT
 PHA:
 CLC:
 ADC FRETOP
 STA FRETOP
 BCC FREPLA
 INC FRETOP+1
FREPLA: .byte PLA
FRETRT: STX INDEX
STY INDEX+1
 RTS:
FRETMS: CPY LASTPT+1
 BNE FRERTS
 CMP LASTPT
 BNE FRERTS
 STA TEMPPT
 SBC #3
 STA LASTPT
 LDY #0
FRERTS: .byte RTS
CHR: JSR CONINT
 TXA:
 PHA:
 LDA #1
 JSR STRSPA
 PLA:
 LDY #0
 STA (DSCTMP+1),Y
 PLA:
 PLA:
RLZRET: JMP PUTNEW
LEFT: JSR PREAM
 CMP (DSCPNT),Y
 TYA:
RLEFT: BCC RLEFT1
 LDA (DSCPNT),Y
 TAX:
 TYA:
RLEFT1: .byte PHA
RLEFT2: .byte TXA
RLEFT3: .byte PHA
 JSR STRSPA
 LDA DSCPNT
LDY DSCPNT+1
 JSR FRETMP
 PLA:
 TAY:
 PLA:
 CLC:
 ADC INDEX
 STA INDEX
 BCC PULMOR
 INC INDEX+1
PULMOR: .byte TYA
 JSR MOVDO
 JMP PUTNEW
RIGHT: JSR PREAM
 CLC:
 SBC (DSCPNT),Y
 EOR #255
 JMP RLEFT
MID: LDA #255
 STA FACLO
 JSR CHRGOT
 CMP #41
 BEQ MID2
 JSR CHKCOM
 JSR GETBYT
MID2: JSR PREAM
 BEQ GOFUC
 DEX:
 TXA:
 PHA:
 CLC:
 LDX #0
 SBC (DSCPNT),Y
 BCS RLEFT2
 EOR #255
 CMP FACLO
 BCC RLEFT3
 LDA FACLO
 BCS RLEFT3
PREAM: JSR CHKCLS
 PLA:
 TAY:
 PLA:
 STA JMPER+1
 PLA:
 PLA:
 PLA:
 TAX:
 PLA
STA DSCPNT+1
PLA
STA DSCPNT
 LDA JMPER+1
 PHA:
 TYA:
 PHA:
 LDY #0
 TXA:
 RTS:
LEN: JSR LEN1
 JMP SNGFLT
LEN1: JSR FRESTR
 LDX #0
 STX VALTYP
 TAY:
 RTS:
ASC: JSR LEN1
 BEQ GOFUC
 LDY #0
 LDA (INDEX1),Y
 TAY:
 JMP SNGFLT
GOFUC: JMP FCERR
GTBYTC: JSR CHRGET
GETBYT: JSR FRMNUM
CONINT: JSR POSINT
 LDX FACMO
 BNE GOFUC
 LDX FACLO
CHRGO2: JMP CHRGOT
VAL: JSR LEN1
 BNE __LJ9
 JMP ZEROFC
__LJ9:
 LDX TXTPTR
LDY TXTPTR+1
 STX STRNG2
STY STRNG2+1
 LDX INDEX1
 STX TXTPTR
 CLC:
 ADC INDEX1
 STA INDEX2
 LDX INDEX1+1
 STX TXTPTR+1
 BCC VAL2
 INX:
VAL2: STX INDEX2+1
 LDY #0
 LDA (INDEX2),Y
 PHA:
 LDA #0
 STA (INDEX2),Y
 JSR CHRGOT
 JSR FIN
 PLA:
 LDY #0
 STA (INDEX2),Y
ST2TXT: LDX STRNG2
LDY STRNG2+1
 STX TXTPTR
STY TXTPTR+1
VALRTS: .byte RTS
GETNUM: JSR FRMNUM
 JSR GETADR
COMBYT: JSR CHKCOM
 JMP GETBYT
GETADR: LDA FACSGN
 BMI GOFUC
 LDA FACEXP
 CMP #145
 BCS GOFUC
 JSR QINT
 LDA FACMO
LDY FACMO+1
 STY POKER
 STA POKER+1
 RTS:
PEEK: LDA POKER
PHA
LDA POKER+1
PHA
 JSR GETADR
 LDY #0
GETCON: LDA (POKER),Y
 TAY:
DOSGFL: PLA
STA POKER+1
PLA
STA POKER
 JMP SNGFLT
POKE: JSR GETNUM
 TXA:
 LDY #0
 STA (POKER),Y
 RTS:
FNWAIT: JSR GETNUM
 STX ANDMSK
 LDX #0
 JSR CHRGOT
 BEQ ZSTORDO
 JSR COMBYT
STORDO: STX EORMSK
 LDY #0
WAITER: LDA (POKER),Y
 EOR EORMSK
 AND ANDMSK
 BEQ WAITER
ZERRTS: .byte RTS
FADDH: LDA #<FHALF
LDY #>FHALF
 JMP FADD
FSUB: JSR CONUPK
FSUBT: LDA FACSGN
 EOR #377
 STA FACSGN
 EOR ARGSGN
 STA ARISGN
 LDA FACEXP
 JMP FADDT
.XCREF:
ZSTORDO = STORDO
.CREF:
FADD5: JSR SHIFTR
 BCC FADD4
FADD: JSR CONUPK
FADDT: BNE __LJ10
 JMP MOVFA
__LJ10:
 LDX FACOV
 STX OLDOV
 LDX #0
 LDA ARGEXP
FADDC: .byte TAY
 BEQ ZERRTS
 SEC:
 SBC FACEXP
 BEQ FADD4
 BCC FADDA
 STY FACEXP
 LDY ARGSGN
 STY FACSGN
 EOR #377
 ADC #0
 LDY #0
 STY OLDOV
 LDX #0
 BNE FADD1
FADDA: LDY #0
 STY FACOV
FADD1: CMP #^D256-7
 BMI FADD5
 TAY:
 LDA FACOV
 LSR 1,X,
 JSR ROLSHF
FADD4: BIT ARISGN
 BPL FADD2
FADD3: LDY #0
 CPX #0
 BEQ SUBIT
 LDY #0
SUBIT: .byte SEC
 EOR #377
 ADC OLDOV
 STA FACOV
 LDA 3+ADDPRC,Y
 SBC 3+ADDPRC,X
 STA FACLO
 LDA 2+ADDPRC,Y
 SBC 2+ADDPRC,X
 STA FACMO
 LDA 2,Y
 SBC 2,X
 STA FACMOH
 LDA 1,Y
 SBC 1,X
 STA FACHO
FADFLT: BCS NORMAL
 JSR NEGFAC
NORMAL: LDY #0
 TYA:
 CLC:
NORM3: LDX FACHO
 BNE NORM1
 LDX FACHO+1
 STX FACHO
 LDX FACMOH+1
 STX FACMOH
 LDX FACMO+1
 STX FACMO
 LDX FACOV
 STX FACLO
 STY FACOV
 ADC #10
 CMP #40
 BNE NORM3
ZEROFC: LDA #0
ZEROF1: STA FACEXP
ZEROML: STA FACSGN
 RTS:
FADD2: ADC OLDOV
 STA FACOV
 LDA FACLO
 ADC ARGLO
 STA FACLO
 LDA FACMO
 ADC ARGMO
 STA FACMO
 LDA FACMOH
 ADC ARGMOH
 STA FACMOH
 LDA FACHO
 ADC ARGHO
 STA FACHO
 JMP SQUEEZ
NORM2: ADC #1
 ASL FACOV
 ROL FACLO
 ROL FACMO
 ROL FACMOH
 ROL FACHO
NORM1: BPL NORM2
 SEC:
 SBC FACEXP
 BCS ZEROFC
 EOR #377
 ADC #1
 STA FACEXP
SQUEEZ: BCC RNDRTS
RNDSHF: INC FACEXP
 BEQ OVERR
 ROR FACHO
 ROR FACMOH
 ROR FACMO
 ROR FACLO
 ROR FACOV
RNDRTS: .byte RTS
NEGFAC: LDA FACSGN
EOR #$FF
STA FACSGN
NEGFCH: LDA FACHO
EOR #$FF
STA FACHO
 LDA FACMOH
EOR #$FF
STA FACMOH
 LDA FACMO
EOR #$FF
STA FACMO
 LDA FACLO
EOR #$FF
STA FACLO
 LDA FACOV
EOR #$FF
STA FACOV
 INC FACOV
 BNE INCFRT
INCFAC: INC FACLO
 BNE INCFRT
 INC FACMO
 BNE INCFRT
 INC FACMOH
 BNE INCFRT
 INC FACHO
INCFRT: .byte RTS
OVERR: LDX #0
 JMP ERROR
MULSHF: LDX #-1
SHFTR2: LDY 3+ADDPRC,X,
 STY FACOV
 LDY 3,X
 STY 4,X
 LDY 2,X,
 STY 3,X,
 LDY 1,X,
 STY 2,X,
 LDY BITS
 STY 1,X,
SHIFTR: ADC #10
 BMI SHFTR2
 BEQ SHFTR2
 SBC #10
 TAY:
 LDA FACOV
 BCS SHFTRT
SHFTR3: ASL 1,X
 BCC SHFTR4
 INC 1,X
SHFTR4: ROR 1,X
 ROR 1,X
ROLSHF:
 ROR 2,X
 ROR 3,X
ROR 4,X
ROR A,
SHFTR7: .byte INY
 BNE SHFTR3
SHFTRT: .byte CLC
 RTS:
FONE: .byte 201
.byte 000
.byte 000
.byte 000
.byte 0
LOGCN2: .byte 3
.byte 177
.byte 136
.byte 126
.byte 313
.byte 171
.byte 200
.byte 023
.byte 233
.byte 013
.byte 144
.byte 200
.byte 166
.byte 070
.byte 223
.byte 026
.byte 202
.byte 070
.byte 252
.byte 073
.byte 040
SQRHLF: .byte 200
.byte 065
.byte 004
.byte 363
.byte 064
SQRTWO: .byte 201
.byte 065
.byte 004
.byte 363
.byte 064
NEGHLF: .byte 200
.byte 200
.byte 000
.byte 000
.byte 0
LOG2: .byte 200
.byte 061
.byte 162
.byte 370
LOG: JSR SIGN
 BEQ LOGERR
 BPL LOG1
LOGERR: JMP FCERR
LOG1: LDA FACEXP
 SBC #177
 PHA:
 LDA #200
 STA FACEXP
 LDA #<SQRHLF
LDY #>SQRHLF
 JSR FADD
 LDA #<SQRTWO
LDY #>SQRTWO
 JSR FDIV
 LDA #<FONE
LDY #>FONE
 JSR FSUB
 LDA #<LOGCN2
LDY #>LOGCN2
 JSR POLYX
 LDA #<NEGHLF
LDY #>NEGHLF
 JSR FADD
 PLA:
 JSR FINLOG
MULLN2: LDA #<LOG2
LDY #>LOG2
FMULT: JSR CONUPK
FMULTT: BNE __LJ11
 JMP MULTRT
__LJ11:
 JSR MULDIV
 LDA #0
 STA RESHO
 STA RESMOH
 STA RESMO
 STA RESLO
 LDA FACOV
 JSR MLTPLY
 LDA FACLO
 JSR MLTPLY
 LDA FACMO
 JSR MLTPLY
 LDA FACMOH
 JSR MLTPLY
 LDA FACHO
 JSR MLTPL1
 JMP MOVFR
MLTPLY: BNE __LJ12
 JMP MULSHF
__LJ12:
MLTPL1: LSR A,
 ORA #200
MLTPL2: .byte TAY
 BCC MLTPL3
 CLC:
 LDA RESLO
 ADC ARGLO
 STA RESLO
 LDA RESMO
 ADC ARGMO
 STA RESMO
 LDA RESMOH
 ADC ARGMOH
 STA RESMOH
 LDA RESHO
 ADC ARGHO
 STA RESHO
MLTPL3: ROR RESHO
 ROR RESMOH
 ROR RESMO
 ROR RESLO
 ROR FACOV
 TYA:
 LSR A,
 BNE MLTPL2
MULTRT: .byte RTS
CONUPK: STA INDEX1
STY INDEX1+1
 LDY #4
 LDA (INDEX1),Y
 STA ARGLO
 DEY:
 LDA (INDEX1),Y
 STA ARGMO
 DEY:
 LDA (INDEX1),Y
 STA ARGMOH
 DEY:
 LDA (INDEX1),Y
 STA ARGSGN
 EOR FACSGN
 STA ARISGN
 LDA ARGSGN
 ORA #200
 STA ARGHO
 DEY:
 LDA (INDEX1),Y
 STA ARGEXP
 LDA FACEXP
 RTS:
MULDIV: LDA ARGEXP
MLDEXP: BEQ ZEREMV
 CLC:
 ADC FACEXP
 BCC TRYOFF
 BMI GOOVER
 CLC:
 SKIP2:
TRYOFF: BPL ZEREMV
 ADC #200
 STA FACEXP
 BNE __LJ13
 JMP ZEROML
__LJ13:
 LDA ARISGN
 STA FACSGN
 RTS:
MLDVEX: LDA FACSGN
 EOR #377
 BMI GOOVER
ZEREMV: .byte PLA
 PLA:
 JMP ZEROFC
GOOVER: JMP OVERR
MUL10: JSR MOVAF
 TAX:
 BEQ MUL10R
 CLC:
 ADC #2
 BCS GOOVER
FINML6: LDX #0
 STX ARISGN
 JSR FADDC
 INC FACEXP
 BEQ GOOVER
MUL10R: .byte RTS
TENZC: .byte 204
.byte 040
.byte 000
.byte 000
.byte 0
DIV10: JSR MOVAF
 LDA #<TENZC
LDY #>TENZC
 LDX #0
FDIVF: STX ARISGN
 JSR MOVFM
 JMP FDIVT
FDIV: JSR CONUPK
FDIVT: BEQ DV0ERR
 JSR ROUND
 LDA #0
 SEC:
 SBC FACEXP
 STA FACEXP
 JSR MULDIV
 INC FACEXP
 BEQ GOOVER
 LDX #^D256-3-ADDPRC
 LDA #1
DIVIDE:
 LDY ARGHO
 CPY FACHO
 BNE SAVQUO
 LDY ARGMOH
 CPY FACMOH
 BNE SAVQUO
 LDY ARGMO
 CPY FACMO
 BNE SAVQUO
 LDY ARGLO
 CPY FACLO
SAVQUO: .byte PHP
 ROL A,
 BCC QSHFT
 INX:
 STA RESLO,X
 BEQ LD100
 BPL DIVNRM
 LDA #1
QSHFT: .byte PLP
 BCS DIVSUB
SHFARG: ASL ARGLO
 ROL ARGMO
 ROL ARGMOH
 ROL ARGHO
 BCS SAVQUO
 BMI DIVIDE
 BPL SAVQUO
DIVSUB: .byte TAY
 LDA ARGLO
 SBC FACLO
 STA ARGLO
 LDA ARGMO
 SBC FACMO
 STA ARGMO
 LDA ARGMOH
 SBC FACMOH
 STA ARGMOH
 LDA ARGHO
 SBC FACHO
 STA ARGHO
 TYA:
 JMP SHFARG
LD100: LDA #100
 BNE QSHFT
DIVNRM: ASL A
 ASL A
 ASL A
 ASL A
 ASL A
 ASL A
 STA FACOV
 PLP:
 JMP MOVFR
DV0ERR: LDX #0
 JMP ERROR
MOVFR: LDA RESHO
 STA FACHO
 LDA RESMOH
 STA FACMOH
 LDA RESMO
 STA FACMO
 LDA RESLO
 STA FACLO
 JMP NORMAL
MOVFM: STA INDEX1
STY INDEX1+1
 LDY #4
 LDA (INDEX1),Y
 STA FACLO
 DEY:
 LDA (INDEX1),Y
 STA FACMO
 DEY:
 LDA (INDEX1),Y
 STA FACMOH
 DEY:
 LDA (INDEX1),Y
 STA FACSGN
 ORA #200
 STA FACHO
 DEY:
 LDA (INDEX1),Y
 STA FACEXP
 STY FACOV
 RTS:
MOV2F: LDX #0
 SKIP2:
MOV1F: LDX #0
MOVML: LDY #0
 BEQ MOVMF
MOVVF: LDX FORPNT
LDY FORPNT+1
MOVMF: JSR ROUND
 STX INDEX1
STY INDEX1+1
 LDY #4
 LDA FACLO
 STA (INDEX),Y
 DEY:
 LDA FACMO
 STA (INDEX),Y
 DEY:
 LDA FACMOH
 STA (INDEX),Y
 DEY:
 LDA FACSGN
 ORA #177
 AND FACHO
 STA (INDEX),Y
 DEY:
 LDA FACEXP
 STA (INDEX),Y
 STY FACOV
 RTS:
MOVFA: LDA ARGSGN
MOVFA1: STA FACSGN
 LDX #5
MOVFAL: LDA ARGEXP-1,X
 STA FACEXP-1,X
 DEX:
 BNE MOVFAL
 STX FACOV
 RTS:
MOVAF: JSR ROUND
MOVEF: LDX #6
MOVAFL: LDA FACEXP-1,X
 STA ARGEXP-1,X
 DEX:
 BNE MOVAFL
 STX FACOV
MOVRTS: .byte RTS
ROUND: LDA FACEXP
 BEQ MOVRTS
 ASL FACOV
 BCC MOVRTS
INCRND: JSR INCFAC
 BNE MOVRTS
 JMP RNDSHF
SIGN: LDA FACEXP
 BEQ SIGNRT
FCSIGN: LDA FACSGN
FCOMPS: ROL A
 LDA #255
 BCS SIGNRT
 LDA #1
SIGNRT: .byte RTS
SGN: JSR SIGN
FLOAT: STA FACHO
 LDA #0
 STA FACHO+1
 LDX #210
FLOATS: LDA FACHO
 EOR #377
 ROL A,
FLOATC: LDA #0
 STA FACLO
 STA FACMO
FLOATB: STX FACEXP
 STA FACOV
 STA FACSGN
 JMP FADFLT
ABS: LSR FACSGN
 RTS:
FCOMP: STA INDEX2
FCOMPN: STY INDEX2+1
 LDY #0
 LDA (INDEX2),Y
 INY:
 TAX:
 BEQ SIGN
 LDA (INDEX2),Y
 EOR FACSGN
 BMI FCSIGN
FOUTCP: CPX FACEXP
 BNE FCOMPC
 LDA (INDEX2),Y
 ORA #200
 CMP FACHO
 BNE FCOMPC
 INY:
 LDA (INDEX2),Y
 CMP FACMOH
 BNE FCOMPC
 INY:
 LDA (INDEX2),Y
 CMP FACMO
 BNE FCOMPC
 INY:
 LDA #177
 CMP FACOV
 LDA (INDEX2),Y
 SBC FACLO
 BEQ QINTRT
FCOMPC: LDA FACSGN
 BCC FCOMPD
 EOR #377
FCOMPD: JMP FCOMPS
QINT: LDA FACEXP
 BEQ CLRFAC
 SEC:
 SBC #238
 BIT FACSGN
 BPL QISHFT
 TAX:
 LDA #377
 STA BITS
 JSR NEGFCH
 TXA:
QISHFT: LDX #0
 CMP #^D256-7
 BPL QINT1
 JSR SHIFTR
 STY BITS
QINTRT: .byte RTS
QINT1: .byte TAY
 LDA FACSGN
 AND #200
 LSR FACHO
 ORA FACHO
 STA FACHO
 JSR ROLSHF
 STY BITS
 RTS:
INT: LDA FACEXP
 CMP #238
 BCS INTRTS
 JSR QINT
 STY FACOV
 LDA FACSGN
 STY FACSGN
 EOR #200
 ROL A,
 LDA #238
 STA FACEXP
 LDA FACLO
 STA INTEGR
 JMP FADFLT
CLRFAC: STA FACHO
STA FACMOH
 STA FACMO
 STA FACLO
 TAY:
INTRTS: .byte RTS
FIN: LDY #0
 LDX #12
FINZLP: STY DECCNT,X
 DEX:
 BPL FINZLP
 BCC FINDGQ
 CMP #45
 BNE QPLUS
 STX SGNFLG
 BEQ FINC
QPLUS: CMP #43
 BNE FIN1
FINC: JSR CHRGET
FINDGQ: BCC FINDIG
FIN1: CMP #48
 BEQ FINDP
 CMP #48
 BNE FINE
 JSR CHRGET
 BCC FNEDG1
 CMP #129
 BEQ FINEC1
 CMP #45
 BEQ FINEC1
 CMP #129
 BEQ FINEC
 CMP #43
 BEQ FINEC
 BNE FINEC2
FINEC1: ROR EXPSGN
FINEC: JSR CHRGET
FNEDG1: BCC FINEDG
FINEC2: BIT EXPSGN
 BPL FINE
 LDA #0
 SEC:
 SBC TENEXP
 JMP FINE1
FINDP: ROR DPTFLG
 BIT DPTFLG
 BVC FINC
FINE: LDA TENEXP
FINE1: .byte SEC
 SBC DECCNT
 STA TENEXP
 BEQ FINQNG
 BPL FINMUL
FINDIV: JSR DIV10
 INC TENEXP
 BNE FINDIV
 BEQ FINQNG
FINMUL: JSR MUL10
 DEC TENEXP
 BNE FINMUL
FINQNG: LDA SGNFLG
 BMI NEGXQS
 RTS:
NEGXQS: JMP NEGOP
FINDIG: .byte PHA
 BIT DPTFLG
 BPL FINDG1
 INC DECCNT
FINDG1: JSR MUL10
 PLA:
 SEC:
 SBC #48
 JSR FINLOG
 JMP FINC
FINLOG: .byte PHA
 JSR MOVAF
 PLA:
 JSR FLOAT
 LDA ARGSGN
 EOR FACSGN
 STA ARISGN
 LDX FACEXP
 JMP FADDT
FINEDG: LDA TENEXP
 CMP #12
 BCC MLEX10
 LDA #144
 BIT EXPSGN
 BMI MLEXMI
 JMP OVERR
MLEX10: ASL A,
 ASL A
 CLC:
 ADC TENEXP
 ASL A,
 CLC:
 LDY #0
 ADC (TXTPTR),Y
 SEC:
 SBC #48
MLEXMI: STA TENEXP
 JMP FINEC
NZ0999: .byte 233
.byte 076
.byte 274
.byte 037
.byte 375
NZ9999: .byte 236
.byte 156
.byte 153
.byte 047
.byte 375
NZMIL: .byte 236
.byte 156
.byte 153
.byte 050
.byte 000
INPRT: LDA #<INTXT
LDY #>INTXT
 JSR STROU2
 LDA CURLIN+1
 LDX CURLIN
LINPRT: STA FACHO
STX FACHO+1
 LDX #220
 SEC:
 JSR FLOATC
 JSR FOUT
STROU2: JMP STROUT
FOUT: LDY #1
FOUTC: LDA #"
 BIT FACSGN
 BPL FOUT1
 LDA #45
FOUT1: STA FBUFFR-1,Y,
 STA FACSGN
 STY FBUFPT
 INY:
 LDA #48
 LDX FACEXP
 BNE __LJ14
 JMP FOUT19
__LJ14:
 LDA #0
 CPX #200
 BEQ FOUT37
 BCS FOUT7
FOUT37: LDA #<NZMIL
LDY #>NZMIL
 JSR FMULT
 LDA #^D256-3*ADDPRC-6
FOUT7: STA DECCNT
FOUT4: LDA #<NZ9999
LDY #>NZ9999
 JSR FCOMP
 BEQ BIGGES
 BPL FOUT9
FOUT3: LDA #<NZ0999
LDY #>NZ0999
 JSR FCOMP
 BEQ FOUT38
 BPL FOUT5
FOUT38: JSR MUL10
 DEC DECCNT
 BNE FOUT3
FOUT9: JSR DIV10
 INC DECCNT
 BNE FOUT4
FOUT5: JSR FADDH
BIGGES: JSR QINT
 LDX #1
 LDA DECCNT
 CLC:
 ADC #10
 BMI FOUTPI
 CMP #13
 BCS FOUT6
 ADC #255
 TAX:
 LDA #2
FOUTPI: .byte SEC
FOUT6: SBC #2
 STA TENEXP
 STX DECCNT
 TXA:
 BEQ FOUT39
 BPL FOUT8
FOUT39: LDY FBUFPT
 LDA #48
 INY:
 STA FBUFFR-1,Y
 TXA:
 BEQ FOUT16
 LDA #48
 INY:
 STA FBUFFR-1,Y
FOUT16: STY FBUFPT
FOUT8: LDY #0
FOUTIM: LDX #200
FOUT2: LDA FACLO
 CLC:
 ADC FOUTBL+2+ADDPRC,Y
 STA FACLO
 LDA FACMO
 ADC FOUTBL+1+ADDPRC,Y
 STA FACMO
 LDA FACMOH
 ADC FOUTBL+1,Y
 STA FACMOH
 LDA FACHO
 ADC FOUTBL,Y
 STA FACHO
 INX:
 BCS FOUT41
 BPL FOUT2
 BMI FOUT40
FOUT41: BMI FOUT2
FOUT40: .byte TXA
 BCC FOUTYP
 EOR #377
 ADC #12
FOUTYP: ADC #47
 INY
 INY
 INY
 INY
 STY FDECPT
 LDY FBUFPT
 INY:
 TAX:
 AND #177
 STA FBUFFR-1,Y
 DEC DECCNT
 BNE STXBUF
 LDA #48
 INY:
 STA FBUFFR-1,Y,
STXBUF: STY FBUFPT
 LDY FDECPT
FOUTCM: .byte TXA
 EOR #377
 AND #200
 TAX:
 CPY #0
 BNE FOUT2
FOULDY: LDY FBUFPT
FOUT11: LDA FBUFFR-1,Y,
 DEY:
 CMP #48
 BEQ FOUT11
 CMP #48
 BEQ FOUT12
 INY:
FOUT12: LDA #43
 LDX TENEXP
 BEQ FOUT17
 BPL FOUT14
 LDA #0
 SEC:
 SBC TENEXP
 TAX:
 LDA #45
FOUT14: STA FBUFFR-1+2,Y,
 LDA #48
 STA FBUFFR-1+1,Y,
 TXA:
 LDX #47
 SEC:
FOUT15: .byte INX
 SBC #12
 BCS FOUT15
 ADC #60
 STA FBUFFR-1+4,Y,
 TXA:
 STA FBUFFR-1+3,Y,
 LDA #0
 STA FBUFFR-1+5,Y,
 BEQ FOUT20
FOUT19: STA FBUFFR-1,Y,
FOUT17: LDA #0
 STA FBUFFR-1+1,Y
FOUT20: LDA #<FBUFFR
LDY #>FBUFFR
FPWRRT: .byte RTS
FHALF: .byte 200
.byte 000
ZERO: .byte 000
.byte 000
.byte 0
FOUTBL: .byte 372
.byte 012
.byte 037
.byte 000
.byte 000
.byte 230
.byte 226
.byte 200
.byte 377
.byte 360
.byte 275
.byte 300
.byte 000
.byte 001
.byte 206
.byte 240
.byte 377
.byte 377
.byte 330
.byte 360
.byte 000
.byte 000
.byte 003
.byte 350
.byte 377
.byte 377
.byte 377
.byte 234
.byte 000
.byte 000
.byte 000
.byte 012
.byte 377
.byte 377
.byte 377
.byte 377
FDCEND:
SQR: JSR MOVAF
 LDA #<FHALF
LDY #>FHALF
 JSR MOVFM
FPWRT: BEQ EXP
 LDA ARGEXP
 BNE FPWRT1
 JMP ZEROF1
FPWRT1: LDX #<TEMPF3
LDY #>TEMPF3
 JSR MOVMF
 LDA ARGSGN
 BPL FPWR1
 JSR INT
 LDA #<TEMPF3
LDY #>TEMPF3
 JSR FCOMP
 BNE FPWR1
 TYA:
 LDY INTEGR
FPWR1: JSR MOVFA1
 TYA:
 PHA:
 JSR LOG
 LDA #<TEMPF3
LDY #>TEMPF3
 JSR FMULT
 JSR EXP
 PLA:
 LSR A,
 BCC NEGRTS
NEGOP: LDA FACEXP
 BEQ NEGRTS
 LDA FACSGN
EOR #$FF
STA FACSGN
NEGRTS: .byte RTS
LOGEB2: .byte 201
.byte 070
.byte 252
.byte 073
.byte 051
expcon: .byte 6
.byte 164
.byte 143
.byte 220
.byte 214
.byte 167
.byte 043
.byte 014
.byte 253
.byte 172
.byte 036
.byte 224
.byte 000
.byte 174
.byte 143
.byte 102
.byte 200
.byte 176
.byte 165
.byte 376
.byte 320
.byte 200
.byte 061
.byte 162
.byte 025
.byte 201
.byte 000
.byte 000
.byte 000
EXPCON: .byte 7
.byte 161
.byte 064
.byte 130
.byte 076
.byte 126
.byte 164
.byte 026
.byte 176
.byte 263
.byte 033
.byte 167
.byte 057
.byte 356
.byte 343
.byte 205
.byte 172
.byte 035
.byte 204
.byte 034
.byte 052
.byte 174
.byte 143
.byte 131
.byte 130
.byte 012
.byte 176
.byte 165
.byte 375
.byte 347
.byte 306
.byte 200
.byte 061
.byte 162
.byte 030
.byte 020
.byte 201
.byte 000
.byte 000
.byte 000
.byte 000
EXP:
 LDA #<LOGEB2
LDY #>LOGEB2
 JSR FMULT
 LDA FACOV
 ADC #120
 BCC STOLD
 JSR INCRND
STOLD: STA OLDOV
 JSR MOVEF
 LDA FACEXP
 CMP #210
 BCC EXP1
GOMLDV: JSR MLDVEX
EXP1: JSR INT
 LDA INTEGR
 CLC:
 ADC #201
 BEQ GOMLDV
 SEC:
 SBC #1
 PHA:
 LDX #5
SWAPLP: LDA ARGEXP,X
 LDY FACEXP,X
 STA FACEXP,X
 STY ARGEXP,X
 DEX:
 BPL SWAPLP
 LDA OLDOV
 STA FACOV
 JSR FSUBT
 JSR NEGOP
 LDA #<EXPCON
LDY #>EXPCON
 JSR POLY
 LDA #0
STA ARISGN
 PLA:
 JSR MLDEXP
 RTS:
POLYX: STA POLYPT
STY POLYPT+1
 JSR MOV1F
 LDA #0
 JSR FMULT
 JSR POLY1
 LDA #<TEMPF1
LDY #>TEMPF1
 JMP FMULT
POLY: STA POLYPT
STY POLYPT+1
POLY1: JSR MOV2F
 LDA (POLYPT),Y
 STA DEGREE
 LDY POLYPT
 INY:
 TYA:
 BNE POLY3
 INC POLYPT+1
POLY3: STA POLYPT
 LDY POLYPT+1
POLY2: JSR FMULT
 LDA POLYPT
LDY POLYPT+1
 CLC:
 ADC #5
 BCC POLY4
 INY:
POLY4: STA POLYPT
STY POLYPT+1
 JSR FADD
 LDA #<TEMPF2
LDY #>TEMPF2
 DEC DEGREE
 BNE POLY2
RANDRT: .byte RTS
RMULZC: .byte 230
.byte 065
.byte 104
.byte 172
RADDZC: .byte 150
.byte 050
.byte 261
.byte 106
RND: JSR SIGN
 TAX:
 BMI RND1
QSETNR: LDA #<RNDX
LDY #>RNDX
 JSR MOVFM
 TXA:
 BEQ RANDRT
 LDA #<RMULZC
LDY #>RMULZC
 JSR FMULT
 LDA #<RADDZC
LDY #>RADDZC
 JSR FADD
RND1: LDX FACLO
 LDA FACHO
 STA FACLO
 STX FACHO
STRNEX: LDA #0
STA FACSGN
 LDA FACEXP
 STA FACOV
 LDA #200
 STA FACEXP
 JSR NORMAL
 LDX #<RNDX
LDY #>RNDX
GMOVMF: JMP MOVMF
COS: LDA #<PI2
LDY #>PI2
 JSR FADD
SIN: JSR MOVAF
 LDA #<TWOPI
LDY #>TWOPI
 LDX ARGSGN
 JSR FDIVF
 JSR MOVAF
 JSR INT
 LDA #0
STA ARISGN
 JSR FSUBT
 LDA #<FR4
LDY #>FR4
 JSR FSUB
 LDA FACSGN
 PHA:
 BPL SIN1
 JSR FADDH
 LDA FACSGN
 BMI SIN2
 LDA TANSGN
EOR #$FF
STA TANSGN
SIN1: JSR NEGOP
SIN2: LDA #<FR4
LDY #>FR4
 JSR FADD
 PLA:
 BPL SIN3
 JSR NEGOP
SIN3: LDA #<SINCON
LDY #>SINCON
GPOLYX: JMP POLYX
TAN: JSR MOV1F
 LDA #0
STA TANSGN
 JSR SIN
 LDX #<TEMPF3
LDY #>TEMPF3
 JSR GMOVMF
 LDA #<TEMPF1
LDY #>TEMPF1
 JSR MOVFM
 LDA #0
STA FACSGN
 LDA TANSGN
 JSR COSC
 LDA #<TEMPF3
LDY #>TEMPF3
GFDIV: JMP FDIV
COSC: .byte PHA
 JMP SIN1
PI2: .byte 201
.byte 111
.byte 017
.byte $14C
.byte 242
TWOPI: .byte 203
.byte 111
.byte 017
.byte $14C
.byte 242
FR4: .byte 177
.byte 000
.byte 000
.byte 0000
.byte 0
SINCON: .byte 5
.byte 204
.byte 346
.byte 032
.byte 055
.byte 033
.byte 206
.byte 050
.byte 007
.byte 373
.byte 370
.byte 207
.byte 231
.byte 150
.byte 211
.byte 001
.byte 207
.byte 043
.byte 065
.byte 337
.byte 341
.byte 206
.byte 245
.byte 135
.byte 347
.byte 050
.byte 203
.byte 111
.byte 017
.byte 332
.byte 242
.byte 241
.byte 124
.byte 106
.byte 217
.byte 23
.byte 217
.byte 122
.byte 103
.byte 211
.byte 315
ATN: LDA FACSGN
 PHA:
 BPL ATN1
 JSR NEGOP
ATN1: LDA FACEXP
 PHA:
 CMP #201
 BCC ATN2
 LDA #<FONE
LDY #>FONE
 JSR FDIV
ATN2: LDA #<ATNCON
LDY #>ATNCON
 JSR POLYX
 PLA:
 CMP #201
 BCC ATN3
 LDA #<PI2
LDY #>PI2
 JSR FSUB
ATN3: .byte PLA
 BPL ATN4
 JMP NEGOP
ATN4: .byte RTS
ATNCON: .byte 13
.byte 166
.byte 263
.byte 203
.byte 275
.byte 323
.byte 171
.byte 036
.byte 364
.byte 246
.byte 365
.byte 173
.byte 203
.byte 374
.byte 260
.byte 020
.byte 174
.byte 014
.byte 037
.byte 147
.byte 312
.byte 174
.byte 336
.byte 123
.byte 313
.byte 301
.byte 175
.byte 024
.byte 144
.byte 160
.byte 114
.byte 175
.byte 267
.byte 352
.byte 121
.byte 172
.byte 175
.byte 143
.byte 060
.byte 210
.byte 176
.byte 176
.byte 222
.byte 104
.byte 231
.byte 072
.byte 176
.byte 114
.byte 314
.byte 221
.byte 307
.byte 177
.byte 252
.byte 252
.byte 252
.byte 023
.byte 201
.byte 000
.byte 000
.byte 000
.byte 000
INITAT: INC CHRGET+7
 BNE CHZGOT
 INC CHRGET+8
CHZGOT: LDA 60000
 CMP #58
 BCS CHZRTS
 CMP #"
 BEQ INITAT
 SEC:
 SBC #48
 SEC:
 SBC #^D256-"0"
CHZRTS: .byte RTS
.byte 128
.byte 79
.byte 199
.byte 82
.byte 88
TYPAUT: LDA #<AUTTXT
LDY #>AUTTXT
 JSR STROUT
INIT:
 LDX #255
 STX CURLIN+1
 LDX #251
 TXS:
 LDA #<INIT
LDY #>INIT
 STA START+1
STY START+1+1
 STA RDYJSR+1
STY RDYJSR+1+1
 LDA #<AYINT
LDY #>AYINT
 STA ADRAYI
STY ADRAYI+1
 LDA #<GIVAYF
LDY #>GIVAYF
 STA ADRGAY
STY ADRGAY+1
 LDA #76
 STA START
 STA RDYJSR
 STA JMPER
 STA USRPOK
 LDA #<FCERR
LDY #>FCERR
 STA USRPOK+1
STY USRPOK+1+1
 LDA #40
 STA LINWID
 LDA #14
 STA NCMWID
 LDX #4
MOVCHG: LDA INITAT-1,X,
 STA CHRGET-1,X,
 DEX:
 BNE MOVCHG
 LDA #3
 STA FOUR6
 TXA:
 STA BITS
 STA LASTPT+1
 PHA:
 STA CNTWFL
 INX:
 STX BUF-3
 STX BUF-4
 JSR CRDO
 LDX #0
 STX TEMPPT
 LDA #<16190
LDY #>16190
 STA MEMSIZ
STY MEMSIZ+1
 STA FRETOP
STY FRETOP+1
TTYW:
ASKAGN:
 LDX #<RAMLOC
LDY #>RAMLOC
 STX TXTTAB
STY TXTTAB+1
 LDY #0
 TYA:
 STA (TXTTAB),Y
 INC TXTTAB
 BNE QROOM
 INC TXTTAB+1
QROOM: LDA TXTTAB
LDY TXTTAB+1
 JSR REASON
 JSR CRDO
 LDA MEMSIZ
 SEC:
 SBC TXTTAB
 TAX:
 LDA MEMSIZ+1
 SBC TXTTAB+1
 JSR LINPRT
 LDA #<WORDS
LDY #>WORDS
 JSR STROUT
 JSR SCRTCH
 LDA #<STROUT
LDY #>STROUT
 STA RDYJSR+1
STY RDYJSR+1+1
 LDA #<READY
LDY #>READY
 STA START+1
STY START+1+1
AUTTXT: .byte ACRLF
.byte 12
.byte $57, $52, $49, $54, $54, $45, $4E, $20
.byte $42, $59, $20, $57, $45, $49, $4C, $41, $4E, $44, $20, $26, $20, $47, $41, $54, $45, $53
 ACRLF:
.byte 0
MEMORY: .byte $4D, $45, $4D, $4F, $52, $59, $20, $53, $49, $5A, $45
.byte 0
TTYWID:
.byte $54, $45, $52, $4D, $49, $4E, $41, $4C, $20
.byte $57, $49, $44, $54, $48
.byte 0
WORDS: .byte $20, $42, $59, $54, $45, $53, $20, $46, $52, $45, $45
 ACRLF:
 ACRLF:
.byte $41, $50, $50, $4C, $45, $20, $42, $41, $53, $49, $43, $20, $56, $31, $2E, $31
 ACRLF:
.byte $43, $4F, $50, $59, $52, $49, $47, $48, $54, $20, $31, $39, $37, $38, $20, $4D, $49, $43, $52, $4F, $53, $4F, $46, $54
 ACRLF:
.byte 0
LASTWR:
.res 100
START = 0